---
"@flow-state-dev/fsdev": patch
---

Headless `status`, `wake`, `watch`, `abort`, and a successful `seed` / `steer` name the board (`flow.id`) and the `CONDUCTOR_REPO` checkout so leftover env cannot hide which board you just read. (LAB-151)
