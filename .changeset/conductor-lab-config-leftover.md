---
---

Loading the conductor lab config (`fsdev conductor --config` / `CONDUCTOR_CONFIG`) refuses a leftover `CONDUCTOR_REPO` that names a different checkout, the same way the PATH bin does. (LAB-151)
