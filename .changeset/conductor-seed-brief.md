---
"@flow-state-dev/fsdev": patch
---

`fsdev conductor` seed takes a brief after the issue id — extra argv words, or more lines in the TUI — and files it with the row so attempt 1 already has the ticket.
