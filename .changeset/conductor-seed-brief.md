---
"@flow-state-dev/fsdev": patch
---

`fsdev conductor` seed takes a brief after the issue id — extra argv words, more lines in the TUI, or the coordinator's `seed_issue` when you already said the ticket — and files it with the row so attempt 1 already has it. (LAB-151)
