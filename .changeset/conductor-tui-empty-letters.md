---
"@flow-state-dev/fsdev": patch
---

An empty `fsdev conductor` board treats typed letters as talk (`question` no longer quits) and shows the line as soon as you send it. Leave with `/quit` or Ctrl-C. (LAB-151)
