---
"@flow-state-dev/fsdev": patch
---

The conductor board selects the row you just filed — in-session seed and a talk turn that starts work land on that issue, the same way `conductor start` does. (LAB-151)
