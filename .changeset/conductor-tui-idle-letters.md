---
"@flow-state-dev/fsdev": patch
---

Letters on an idle `fsdev conductor` row talk. Arrows move, `r` still refreshes, and `f` / `h` / `e` / `H` still expand the attempt. `/wake` and `/quit` stay the verbs. (LAB-151)
