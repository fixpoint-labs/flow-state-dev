---
"@flow-state-dev/claude-code": patch
---

`createWorkspaceAgent` is the workspace chain as a sequencer step — the same hydrate, run, and flush `createWorkspaceAgentCapability` already installs as a tool.
