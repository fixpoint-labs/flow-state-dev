---
"@flow-state-dev/engine": patch
---

The debug resource endpoints no longer list, count, or return another user's user-owned hired seats, and fetching content for a collection topic that doesn't match the collection's pattern now returns 404 (FIX-1535).
