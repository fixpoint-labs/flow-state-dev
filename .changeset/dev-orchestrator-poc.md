---
---

Add `labs/dev-orchestrator` (internal POC): a single-issue dev-loop orchestrator that drives a Linear issue through its spec stage by dispatching Claude Code and gating on durable suspensions, with a pure stage machine and deterministic Linear/GitHub signal clients. Private dogfooding app — no published-package changes.
