---
"@flow-state-dev/devtool": patch
"@flow-state-dev/node": patch
---

Fix standalone DevTool focus handler reverting Settings when `fsdev.config.ts` injects identity, and use case-insensitive HTML tag matching when injecting DevTool config.
