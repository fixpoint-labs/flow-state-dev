---
---

Internal only, no release (FIX-1159). The install-fsd skill, wiring contract, and existing-project docs live in the private plugin workspace and the docs site. The one published-package touch is a sentence on the CLI README next-steps section that already shipped with sub-PR b. Verified against the diff: no runtime surface under `packages/` changes.
