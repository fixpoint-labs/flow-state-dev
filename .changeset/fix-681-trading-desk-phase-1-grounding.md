---
---

Internal-only: harden the trading-desk example's Phase 1 data layer (a `dataQuality` sentinel on analyst memos, a hard-stop guard when a non-substitutable analyst fails, and two-tier Polymarket coverage with graceful `coverageQuality` gating). No published package changes — the example package is private.
