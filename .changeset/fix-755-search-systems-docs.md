---
---

Docs: clarify that generator-native web search (`GeneratorSearchConfig`) and the `tools.search` tool are separate systems — `tier` is `tools.search`-only, and the two `searchDepth` fields differ (`low`/`medium`/`high` vs `basic`/`advanced`). No code changes.
