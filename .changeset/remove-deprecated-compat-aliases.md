---
"@flow-state-dev/contracts": minor
"@flow-state-dev/core": minor
"@flow-state-dev/react": minor
"@flow-state-dev/patterns": minor
"@flow-state-dev/memory": minor
"@flow-state-dev/vercel": minor
---

Remove unused deprecated aliases and compatibility shims (`AgentType`, `ContextItem`, `ToolBinding`, `resolveNamespaceKey`, `ResourceHandle`, `CollectionItem`, `PlanStep`/`PlanStepSchema`, legacy plan types, `reviewOutputSchema`, `PRE_RANK_CAP`, the Vercel `heartbeatMs` no-op, and the `FSDEV_DEBUG_ITEMS` env fallback). Use the canonical replacements already documented on each surface.
