---
"@flow-state-dev/react": patch
---

`ItemsRenderer` provides the session item list to nested stream-aware components when none is already in scope, and exports `SessionItemsProvider` / `useSessionItems` (FIX-1498).
