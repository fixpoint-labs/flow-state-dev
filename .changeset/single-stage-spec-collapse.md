---
---

Internal (docs/skills): the spec is now authored in a **single stage** and its Build Plan (Part II) is now **directional** rather than an exhaustive blueprint. Part II fixes the shape and sequence — which modules/layers are involved, how they fit, the order to build them — and prefers a mermaid diagram over code, allowing only small explicitly-illustrative snippets; the implementer owns the exact signatures, code, and line-level choices (with `tdd`/`diagnose` and the challenger). Because Part II is cheap to write once it's directional, the two-stage draft-gated flow collapses: `issue-spec` drafts Part I and Part II together and opens the spec PR **ready for review** at a single spec-approval gate — the draft→promote mechanics, the separate Case-approval stage, and `Step 6.6` are gone. The orchestrators follow suit: `issue-lifecycle` merges `AWAITING_CASE_APPROVAL` + `AWAITING_SPEC_APPROVAL` into one phase and drops the spec-PR `draft`-flag derivation; `issue-implement` drops the Part II-pending/draft readiness check; `issue-fleet` drops the Case-approval gate; and `docs/contributing/orchestration.md` (+ its Gates table and diagram), the spec template, and `AGENTS.md` are updated to match. One spec-approval gate in, one merge gate out.
