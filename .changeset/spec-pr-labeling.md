---
---

Internal (skills): `issue-spec` labels the spec PR `spec` on creation and applies the `spec approved` label on sign-off; `issue-lifecycle` closes the spec PR at the approval gate once the label is present.
