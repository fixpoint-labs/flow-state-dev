---
"@flow-state-dev/workforce": minor
---

Hiring now hands every worker seat the same settings — its instructions, its team's, and the skills its folders declared — so a custom worker kind must accept them by composing the new `workerConfigSchema()` into its `configSchema`, or it refuses the whole roster at startup (FIX-1367).
