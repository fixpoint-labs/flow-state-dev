---
review_agents: [kieran-typescript-reviewer, code-simplicity-reviewer, security-sentinel, performance-oracle]
plan_review_agents: [kieran-typescript-reviewer, code-simplicity-reviewer]
---

# Review Context

This is a TypeScript block-based AI workflow framework.
- Core types and models are in `packages/core`.
- Server implementation is in `packages/engine`.
- Client implementation is in `packages/client`.
- React hooks are in `packages/react`.
- Example usage is in `apps/kitchen-sink`.
- Ensure new features are agent-accessible.
