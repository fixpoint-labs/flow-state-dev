# Best Practices (Living)

Purpose:

- Capture implementation standards decided during planning/review.
- Keep decisions cumulative so future work follows the same bar.

## How this is organized

Best practices have two altitudes:

- **Universal** — apply to any change in any package; every agent should weigh
  them. Their full text lives below and is mirrored into the always-loaded
  `CLAUDE.md` so they are in context for every task.
- **Situational** — apply only when working in a named area. Their full text
  lives in per-category files under [`best-practices/`](best-practices/); only
  the index below (and a copy in `CLAUDE.md`) is always loaded. Open the
  category file when you're working in that area.

BP numbers are global IDs and never change file-to-file. A superseded practice
is marked `Superseded` and links its replacement; it is never deleted.

Update policy:

- A practice is established by **user review** (see the `fsd:distill-lessons`
  skill). Add it in the same change set as the code/docs adopting it.
- Number sequentially after the last existing BP (currently BP-035). Append;
  never overwrite or renumber.
- A new **universal** BP goes in this file's Universal section *and* the
  `CLAUDE.md` mirror. A new **situational** BP goes in the matching
  `best-practices/<category>.md` file, and its one-line entry goes in the index
  below *and* the `CLAUDE.md` index.
- Keep entries terse — the rule, not the proof. The file is loaded into agent
  context: no `Why`/proof section. Ground a rule in a real incident as your gate
  for adding it, but don't write the proof into the entry.

---

## Universal Practices

### BP-001: Documentation authority precedence

- Status: Active
- Date: 2026-02-15
- Scope: Universal — resolving in-repo doc conflicts.
- Rule:
  - If two in-repo docs conflict, the more specific reference wins (e.g. `docs/architecture/streaming.md` over a general statement in `overview.md`).
  - Planning and wave docs reference the relevant `docs/architecture/<area>.md` file rather than restating contracts inline.

### BP-003: Verification evidence is mandatory

- Status: Active
- Date: 2026-02-15
- Scope: Universal — every claimed deliverable.
- Rule:
  - Every claimed deliverable must have an evidence path and pass criteria.
  - Wave close-out requires a journal and changelog under `docs/waves/`.

### BP-007: Concise API and file-level documentation

- Status: Active
- Date: 2026-02-16
- Scope: Universal — any implementation file.
- Rule:
  - Add a concise file header comment explaining the file's role in the runtime.
  - Document 100% of exported methods/functions/classes with concise doc comments focused on contract and behavior.
  - Document important internal helpers carrying non-obvious control flow, state transitions, or error semantics.
  - Keep comments high-signal and short; avoid restating obvious syntax.

### BP-022: Release notes via Changesets

- Status: Active
- Date: 2026-05-19
- Scope: Universal — any PR with user-facing impact.
- Rule:
  - Every PR with user-facing impact (new/changed public API, capability, block, CLI command, hook, env var, config key, or observable behavior) includes a `.changeset/*.md` fragment listing every affected publishable package.
  - Pre-1.0: select `patch` for non-breaking, `minor` for new capabilities or breaking changes. Never `major` (it bumps a `0.x` package to `1.0.0`); raise it in the PR if you believe it's warranted.
  - Internal-only changes need no changeset: `pnpm changeset --empty` or state "no changeset needed".
  - Do not edit a root `changelog.md` (none exists); per-package `CHANGELOG.md` files are generated by `pnpm version-packages`.
- See: [`release-notes-workflow.md`](release-notes-workflow.md)
- Supersedes: BP-005

### BP-028: Fix the bug at the layer that owns it, not where it bit you

- Status: Active
- Date: 2026-06-27
- Scope: Universal — applies to any change in any package; every agent should weigh it.
- Rule:
  - When a failure forces a workaround at the call site — stripping an env var before spawning a subprocess, catching-and-massaging a framework error, guarding around a surprising default — treat that as a signal the bug likely lives one layer down. Before patching where it bit you, ask: "Which layer *owns* this behavior, and would fixing it there cure it for every caller?"
  - Push the fix to the owning layer whenever the same failure would recur for any other consumer in the same situation (another app on the framework, a shared/CI/automation environment, the next tool that spawns the same process). A workaround each caller has to repeat is a smell.
  - A leaf workaround is acceptable only when the owning layer genuinely can't change (third-party, or out of scope for the current work) — and then say so inline: document why the fix lives where it does.

### BP-029: Prefer composing existing primitives over re-implementing what a tool already provides

- Status: Active
- Date: 2026-06-27
- Scope: Universal — applies to any change in any package; every agent should weigh it.
- Rule:
  - Before writing glue that wraps an existing capability (a CLI command, a framework primitive, a documented workflow), ask: "Could a documented recipe over the existing commands replace this code entirely?" If yes, write the recipe — a skill, a runbook, a `goals/` check — not the wrapper.
  - Apply the compose-don't-build / necessity discipline to *plumbing*, not just headline features. Every line of bespoke orchestration is a line that can harbor a bug — and reviewers/owners tend to delete it.
  - Reserve bespoke code for the part that genuinely doesn't exist yet — a *new primitive* (e.g. a structured, machine-readable read-back surface), not a re-implementation of subprocess spawning, capture parsing, exit-code handling, or concurrency the tool already does.
  - Automate the *invariant*, teach the *workflow*: a deterministic check belongs in a script (a `goals/` smoke test); an exploratory sequence of existing commands plus judgment about when to escalate cost belongs in a skill/doc.

### BP-030: Tolerate the old shape when you change a persisted or in-flight field

- Status: Active
- Date: 2026-06-27
- Scope: Universal — any change to a persisted or in-flight data shape (resource state, request `metadata`, stored records, client-store buffers).
- Rule:
  - Dual-read the old shape for the rollout window: `metadata.schedule?.scheduleId ?? metadata.scheduleId` (optional-chain the new path so a legacy record falls back instead of throwing). Never assume every record on disk or in flight already has the new shape.
  - Renaming or removing a key from a public config object: reject the dead key with an error pointing at the replacement (`"reactTo.updated was renamed to reactTo.stateUpdated"`). Never accept-then-silently-ignore an unknown key.
  - Adding a nullable field for back-compat: every downstream reader (formatter / renderer / prompt-context builder) must `== null`-guard it.
  - Pair any transitional dual-read with a cleanup pointer (`TODO(FIX-NNN)` or changeset note) so it doesn't outlive the rollout.

### BP-034: Finish move/rename refactors — update provenance, not just imports

- Status: Active
- Date: 2026-06-27
- Scope: Universal — package moves, renames, and extractions.
- Rule:
  - A move that compiles isn't done. Update what the move invalidated: file-header comments naming the old home, architecture/dependency diagrams, and doc examples / cross-link anchors.
  - Re-export moved public types from every documented import path, including subpath barrels (`@flow-state-dev/core/types`), not just the package root.

### BP-035: Walk the second-path checklist before declaring a change done

- Status: Active
- Date: 2026-06-27
- Scope: Universal — review discipline for any change.
- Rule:
  - For the changed surface, handle (or explicitly scope out) each path the change *touches but didn't add code for*:
    - Legacy / persisted records (BP-030).
    - Null / empty / boundary inputs; `NaN`/`Infinity` through `z.number()` (use `.finite()`); guard-clause order (expiry before validation).
    - Concurrent / duplicate calls (409 windows) — guard re-entrancy in the handler, not only via a disabled button.
    - Cancel / error paths — `ctx.signal` threaded; cleanup on synchronous throw; a record exists before any id is returned.
    - Second tenant / account — keys scoped to the full tuple (BP-031).
    - Cost / observability — every model call counted and cancelable (BP-032).
    - React derived state — flags derived from the *complete* input set; change-signals fire only on a *real* change (BP-010).
  - When a flag toggles behavior (`enabled: false`, a new mode), add the test for the off / new state in the same PR.

---

## Situational Practices — Index

Full text lives in the category files. Open the file when working in that area.

### [Process & Docs](best-practices/process.md)

| BP | Rule |
| --- | --- |
| BP-002 | Wave-driven execution — every change maps to a wave task with deliverables + verification |
| BP-004 | Public boundary first — lock import/export shape before behavior depth |
| BP-005 | *(Superseded by BP-022)* dual changelog requirement |
| BP-006 | Keep wave labels out of code and tests |
| BP-008 | Keep root README onboarding-first and current |
| BP-009 | Maintain package-level READMEs for public packages |

### [Blocks & Composition](best-practices/blocks.md)

| BP | Rule |
| --- | --- |
| BP-011 | Handlers must not call blocks internally — compose as a sequencer |
| BP-012 | Use `.tap()` for state-mutation-only blocks — never return input |
| BP-013 | Use `connectInput`/`connectOutput` inside the router, not on blocks |
| BP-014 | Handlers must never return input as output |
| BP-024 | Helpers when the body varies; factories when only identity varies |
| BP-025 | Declare and validate sequencer output schemas deliberately |

### [Generators & Prompts](best-practices/generators.md)

| BP | Rule |
| --- | --- |
| BP-016 | Generator outputSchemas must be OpenAI strict-compatible |
| BP-017 | Use the generator `context` slot for typed, segmented prompts |
| BP-018 | Shared prompt formatters live in `lib/` |
| BP-032 | A secondary model/tool call inherits the primary call's runtime contract |

### [Resources & State](best-practices/resources.md)

| BP | Rule |
| --- | --- |
| BP-015 | Prefer `expose`/`exclude` over hand-rolled `data` projections |
| BP-019 | Resource refs live in dedicated leaf modules |
| BP-020 | Live mode never silently falls back to fixture data |
| BP-021 | Tool blocks declare `cacheable` deliberately |
| BP-023 | Resource state schemas use `.nullable().default(null)` |
| BP-027 | User-scoped resources default to shared; isolate only deliberately |
| BP-033 | Filter before you load on lazy resource collections |

### [React](best-practices/react.md)

| BP | Rule |
| --- | --- |
| BP-010 | React component conventions (`useMemo` over `useEffect`; complete-input derived state) |

### [Engine & Transport](best-practices/engine.md)

| BP | Rule |
| --- | --- |
| BP-026 | Bundle forwarded options into a `RuntimeConfig`-shaped struct, never drill |
| BP-031 | Key auth/routing off the trusted transport `source`, not caller `metadata` |

---

## Template For New Entries

Keep entries terse — the rule, not the proof. These are decided guidelines, not
arguments, and the file is loaded into agent context: no `Why`/proof section.
Ground a rule in a real incident as your gate for adding it, but don't write the
proof into the entry. Place the entry in the Universal section (and the
`CLAUDE.md` mirror) or in the matching `best-practices/<category>.md` file, and
add its index row above (and in `CLAUDE.md`).

```md
### BP-XXX: <Name>

- Status: Active | Superseded
- Date: YYYY-MM-DD
- Scope: Universal | <situation it applies to>
- Rule:
  - ...
- Superseded by: BP-YYY (optional)
```
