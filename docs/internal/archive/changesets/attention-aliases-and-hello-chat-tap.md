---
---

Internal only: removes seven unreachable export aliases from `@thought-fabric/core`'s attention blocks (no import path could address them), converts the `hello-chat` example's counter to `.tap()`, and fixes the `docs/architecture/blocks.md` snippets that taught the same identity-handler shape. No published package surface changes.
