---
"@flow-state-dev/devtool": patch
---

The block-trace inspector now shows a "Template Source" section with the raw `.md` source for generators whose prompt was authored as a prompt file, alongside the rendered prompt.
