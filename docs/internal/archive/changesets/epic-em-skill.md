---
---

Internal (skills): add `epic-em` (FIX-1073) — an engineering-manager posture over `epic-lifecycle` for running an epic without bottlenecking on the user's approval. It moves no gate (epic objective, spec approval and merge stay the user's) and adds no state; what changes is that an escalated *engineering* blocker is decided and recorded rather than surfaced — while product-critical and architecturally-critical calls, `INCONCLUSIVE` POC verdicts and epic-level questions still go up, and deciding never means reading a spec or diff in the coordinator. Registered in `CLAUDE.md` with a one-line back-link from `epic-lifecycle`.
