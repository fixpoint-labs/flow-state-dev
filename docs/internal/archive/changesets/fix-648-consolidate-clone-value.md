---
"@flow-state-dev/core": patch
---

Add `cloneValue` to `@flow-state-dev/core/helpers`, giving the deep-clone primitive a single canonical home alongside `deepMerge` and `deepEqual`. The three state-shape helpers are now documented together as part of the core helpers surface.
