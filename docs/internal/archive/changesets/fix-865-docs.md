---
"@flow-state-dev/client": patch
---

Document `continue()` / `continueStream()` on the recovery client, add the `continuation` item to the SSE item-type reference, and extend the DevTool overview with an "Interrupted runs and crash recovery" section covering the per-run Continue action and how to read the crash-recovery boundary divider.
