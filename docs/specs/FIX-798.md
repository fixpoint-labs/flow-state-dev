# FIX-798: Trading-desk: add Alpha Vantage as a data provider — Implementation Spec

## 1. TLDR

**Plain-language summary.** The trading desk pulls market data from several outside services (Finnhub, Yahoo, FRED, and others). A few of its data tools have only one source, so when that source has no key or fails, the desk gets nothing. Two tools were half-built against a service that was never finished and return empty in live mode. This adds **Alpha Vantage** as a new data source that fills those gaps: it finishes the two half-built tools (earnings-call transcripts and analyst estimates) and gives the insider-trades tool a second source to fall back on. Alpha Vantage's free plan only allows 25 requests a day, so this also adds a simple in-process daily-usage guard that stops calling it once the day's budget is spent (one setting, `ALPHAVANTAGE_DAILY_LIMIT`, turns it off by setting it to 0 for a paid plan). It does **not** try to make Alpha Vantage the *preferred* source anywhere, or wire it into every tool — that's deliberately left to follow-up work.

**Technical statement.** Add an `alpha-vantage` provider module (key gate, a shared request helper that treats AV's HTTP-200 `Note`/`Information` (quota) and `Error Message` (request-shape) bodies as distinct failures, and a race-free in-process daily-budget guard governed by a single `ALPHAVANTAGE_DAILY_LIMIT` knob), then wire it as the live provider for the two stubbed tools (`get_earnings_transcript`, `get_analyst_estimates`) and as a terminal fallback for `get_insider_transactions`.

**Deliverables:**

- **Provider (foundation):** Add `labs/trading-desk/lib/providers/alpha-vantage.ts` — `hasAlphaVantageKey()`, a shared `alphaVantageRequest()` helper (rate-limit-body → throw + race-free budget reserve), a single-knob (`ALPHAVANTAGE_DAILY_LIMIT`, `0`=off) in-process daily-budget guard (`_resetBudget()` test hook), and the fetch functions for transcript, analyst enrichment, and insider transactions.
- **Schema:** Add `"alphavantage"` to the `sourceTag` enum in `labs/trading-desk/flows/analysis/tools/schemas.ts`.
- **Stub 1 — transcript:** Rewrite `get_earnings_transcript.ts` to fetch AV `EARNINGS_CALL_TRANSCRIPT` (latest reported quarter resolved via an `EARNINGS` probe) in live mode.
- **Stub 2 — estimates:** Extend `get_analyst_estimates.ts` to enrich the Finnhub baseline with AV `OVERVIEW` (price targets) + `EARNINGS_ESTIMATES` (forward consensus).
- **Fallback — insider:** Add AV `INSIDER_TRANSACTIONS` as the terminal fallback in `get_insider_transactions.ts`, before `emptyPayload`.
- **Tests:** Add `test/alpha-vantage.spec.ts` (request helper, rate-limit-body, budget guard) + per-tool live-path specs.
- **Docs:** Add `ALPHAVANTAGE_API_KEY` + `ALPHAVANTAGE_DAILY_LIMIT` (with the burst/serverless caveats) to `labs/trading-desk/README.md`; update the three tool-inventory bullets; add `ALPHAVANTAGE_API_KEY` to the provider-keys paragraph in `apps/docs/guides/trading-desk-walkthrough.md`.

**Size estimate.** **Medium** (1 provider module + 3 tool edits + schema + tests + docs; ~350–500 LOC; 1 PR). Optionally splittable as *provider module + budget → then tool wiring*, but cohesive enough for one PR.

## 2. Overview

Linear issue: [FIX-798](https://linear.app/fixpoint-labs/issue/FIX-798/trading-desk-add-alpha-vantage-as-a-data-provider-fallback-coverage)

The desk's live data layer has single-provider dead-ends (a tool degrades straight to an empty payload when its one provider fails) and two tools that were scaffolded against FMP but never wired, so they return `unavailable` in live mode. This spec adds Alpha Vantage — a NASDAQ-licensed source whose free tier covers company overview, the three statements, earnings estimates, earnings-call transcripts, insider transactions, news sentiment, and economic indicators — as the foundation that closes those gaps, under a daily-budget discipline that keeps the free tier's 25-requests/day cap from being silently burned. It is deliberately the **foundation**: it establishes the provider module, the budget machinery, and the `alphavantage` provenance tag that the whole AV follow-on family (FIX-799/800/801/802/803/804) builds on.

## 3. Background & Research

### The Alpha Vantage free-tier constraint (the reason this is not "just add a provider")

The AV free tier is **25 requests/day, 5 requests/minute** (verified June 2026; the daily cap fell 500 → 100 → 25 over time). A full-preset desk run fans ~20 generator calls across nine analysts; naive wiring would exhaust 25/day in a single multi-ticker session. Two facts shape the design:

1. **AV signals failures with an HTTP 200 body**, not an error status: a quota throttle as `{ "Note": "...call frequency..." }` or `{ "Information": "...premium..." }`, and a request-shape error (e.g. a bad `quarter` label) as `{ "Error Message": "..." }`. A fetcher that trusts the status code would treat a throttle or shape message as data. The shared request helper must detect these bodies and throw — throttles as `AlphaVantageRateLimitError`, `Error Message` as the distinct `AlphaVantageRequestError` (§4), since they drive different fallback behavior. This is the single most important correctness property of the provider.
2. **The fetch cache (`lib/cache.ts`) is process-scoped, 120s TTL** — it collapses concurrent same-key reads within a run but does not persist across runs, so AV calls are unprotected day-over-day.

The Note-body handling already gives **honest degradation** on real exhaustion (the tool sees a throw → degrades to the next fallback / `emptyPayload`, tagged `unavailable` in the transcript — visible, not silent). The budget guard adds *proactive* avoidance: stop spending calls that will fail anyway, and keep a single process's usage bounded. Cross-run/persistent budget enforcement is a bigger concern that belongs with the composition/procurement work (FIX-675) and the (now-shipped, but per-session) spine (FIX-758) — out of scope here (see §8, §11).

### Verified AV endpoint shapes (against the live `demo` endpoint)

- **`INSIDER_TRANSACTIONS`** → top-level `data[]`; each element `{ transaction_date, ticker, executive, executive_title, security_type, acquisition_or_disposal, shares, share_price }` (all strings). No SEC transaction code (P/S/A/M) and no separate filing date — the map to the desk's `insiderTransactionItem` is lossy and approximated (see §4).
- **`EARNINGS_CALL_TRANSCRIPT`** → `{ symbol, quarter, transcript[] }`; each element `{ speaker, title, content, sentiment }`. Requires a `quarter` query param (e.g. `2024Q1`) — the fetcher resolves it via an `EARNINGS` probe (see §4), and the fiscal-vs-calendar label needs confirming against the live API.
- **`OVERVIEW`** → includes `AnalystTargetPrice` and `AnalystRatingStrongBuy/Buy/Hold/Sell/StrongSell` (all strings). Supplies the price-target consensus.
- **`EARNINGS_ESTIMATES`** → forward EPS / revenue consensus with analyst counts. Supplies `consensusEstimates`.

### How the codebase already solves this

The desk has a mature **single-provider module convention** (`lib/providers/{finnhub,fred,massive,xai,fmp}.ts`): each reads its own env key, exposes `hasXKey()`, and its fetch functions **throw on any failure** so a calling tool falls through with one `try {} catch {}`. `finnhub.ts` is the closest template (reads `FINNHUB_API_KEY` itself, throws on non-2xx/parse). The tool dispatch convention is `resolveToolPayload(tool, input, ctx, fetchLive)` (`runtime/resolve.ts`) branching fixture / live / record; the live closure gates on `hasXKey()`, tries providers in order, and ends in `emptyPayload`. `get_fundamentals` (Finnhub → Yahoo → empty) is the canonical multi-provider chain; AV simply becomes the terminal link where its shape fits.

### Why this approach over alternatives

- **AV as terminal fallback, never primary.** "AV as candidate primary" and per-signal preference are the **FIX-675** bake-off's call (Backlog). Making AV primary here would pre-empt a data-quality decision this spec has no evidence for. AV stays last in each chain (and *completes* the two stubs, where it's the only live provider).
- **Do not consume the FIX-805 resolver.** The generalized availability resolver (FIX-805) is Backlog; consuming it would block FIX-798 on unbuilt work. Use the established per-tool `try/catch` convention — FIX-805 later refactors all chains uniformly (this tool included).
- **In-process daily-budget guard, not DB-backed.** The provider layer is currently pure HTTP with no persistence, and the desk's DB (PGlite/Postgres) is reached only from `db/` + server. A process-scoped counter (reset on UTC-day roll, disabled by setting `ALPHAVANTAGE_DAILY_LIMIT=0`) is proportionate: the Note-body handling is the real exhaustion safety net; persistent cross-run accounting is FIX-675/procurement territory. Documented limitation, not a silent gap.
- **Fallback list scoped to clean, unowned shape matches.** Insider (`INSIDER_TRANSACTIONS`) maps 1:1 and no follow-on owns it. Macro-via-AV is deferred to **FIX-802** (AV covers only ~4 of the 9 FRED series `get_macro_indicators` needs, each a separate endpoint — budget-expensive for a partial fill), and social/news-via-AV to **FIX-799** (`get_social_sentiment` is X/Twitter chatter; AV `NEWS_SENTIMENT` is news articles — a different modality, dishonest to merge). Confirmed with the issue owner.

### Key Decisions & Ramifications (top 5)

1. **AV is terminal fallback + stub-completer, never a primary or preferred source.** *Alternative rejected:* make AV primary for fundamentals/overview per the paid-tier framing. *Ramification:* keeps FIX-798 independent of the FIX-675 bake-off (Backlog) and evidence-free of a data-quality claim; the "AV as primary" upside is unrealized until FIX-675 lands.
2. **Rate-limit detection lives in one shared `alphaVantageRequest()` helper.** *Alternative rejected:* per-fetcher Note checks. *Ramification:* every AV fetch (this issue's and all follow-ons') inherits correct exhaustion handling for free; the helper is the load-bearing contract the AV family depends on.
3. **Process-scoped daily-budget guard, single `ALPHAVANTAGE_DAILY_LIMIT` knob (0=off); no DB, no tier concept.** *Alternative rejected:* DB-backed cross-run counter, and a separate `ALPHAVANTAGE_TIER` flag. *Ramification:* simple, no new provider-layer dependency, no speculative tier vocabulary (BP-038); correct-enough given Note-handling; does **not** enforce 25/day across process restarts (documented). Cross-run persistence deferred to FIX-675.
4. **Complete the two stubs via AV, not FMP.** *Alternative rejected:* finally wire the FMP module the stubs were scaffolded for. *Ramification:* one provider (AV) covers both stubs + insider, so we don't stand up a second new provider (FMP) in this issue; the `fmp.ts` key-check stub becomes dead code (left in place, not removed — pre-existing, out of this diff's ownership) and README FMP-required wording is corrected to AV.
5. **Fallback list = insider + the two stubs only; macro and social deferred.** *Alternative rejected:* wire all of outcome #3's insider+macro+social. *Ramification:* avoids modality-dishonest social wiring and budget-expensive partial macro fills, at the cost of `get_macro_indicators`/`get_social_sentiment` keeping their current single-provider dead-ends until FIX-802/FIX-799. Confirmed with the owner.

## 4. Technical Design

### Architecture

All changes are inside `labs/trading-desk` except one published-guide docs edit. No framework packages change.

```
lib/providers/alpha-vantage.ts              → NEW provider module (key gate, request helper, budget guard, fetchers)
flows/analysis/tools/schemas.ts             → add "alphavantage" to sourceTag enum
flows/analysis/tools/data/get_earnings_transcript.ts  → live path = AV EARNINGS_CALL_TRANSCRIPT
flows/analysis/tools/data/get_analyst_estimates.ts    → enrich Finnhub baseline with AV OVERVIEW + EARNINGS_ESTIMATES
flows/analysis/tools/data/get_insider_transactions.ts → add AV terminal fallback before emptyPayload
flows/analysis/agents/analysts/prompts/disclosure.prompt.md → drop "no FMP key" / "FMP-only fields" — AV now backs transcripts + estimates
flows/analysis/agents/analysts/prompts/news.prompt.md       → note AV insider rows carry a blank transactionCode (direction in the share sign)
test/alpha-vantage.spec.ts                  → NEW provider unit tests
test/get-earnings-transcript.spec.ts        → NEW (or extend) tool live-path test
test/get-analyst-estimates.spec.ts          → NEW (or extend)
test/get-insider-transactions.spec.ts       → extend with the AV-fallback path
README.md, CLAUDE.md, apps/docs/guides/trading-desk-walkthrough.md  → docs
```

### API surface — the provider module

```ts
// lib/providers/alpha-vantage.ts

const AV_BASE = "https://www.alphavantage.co/query";

/** True when an Alpha Vantage key is configured. */
export function hasAlphaVantageKey(): boolean {
  return Boolean(process.env.ALPHAVANTAGE_API_KEY?.trim());
}

/** Shared request. Injects `apikey=ALPHAVANTAGE_API_KEY` (callers pass only
 *  the endpoint params — `function`, `symbol`, etc.), reserves one unit of the
 *  daily budget synchronously (atomic check-and-increment, before any await —
 *  see below) then fetches, and treats AV's HTTP-200 rate-limit / exhaustion /
 *  error BODIES as failures (throw). Throws AlphaVantageBudgetError when the
 *  day's budget is already spent; on a Note/Information body (quota throttle)
 *  throws AlphaVantageRateLimitError; on an Error Message body (request-shape)
 *  throws the distinct AlphaVantageRequestError; and throws on non-2xx or a
 *  JSON parse failure. */
export async function alphaVantageRequest(
  params: Record<string, string>,   // endpoint params only; apikey is added here
): Promise<Record<string, unknown>>;

// Fetchers — each throws on any failure so the calling tool falls through
// with one try/catch, per the provider convention.
export async function fetchAlphaVantageEarningsTranscript(
  input: { ticker: string; date: string },
): Promise<ToolOutput<"get_earnings_transcript">>;

export async function fetchAlphaVantageAnalystEnrichment(
  ticker: string,
): Promise<Pick<ToolOutput<"get_analyst_estimates">, "consensusEstimates" | "priceTargets">>;

export async function fetchAlphaVantageInsiderTransactions(
  input: { ticker: string; date: string },
): Promise<ToolOutput<"get_insider_transactions">>;

/** Test hook — reset the in-process daily counter between specs. Not in the barrel. */
export function _resetBudget(): void;
```

**Budget guard (in-process, race-free) — one knob, no tier concept.** A single env `ALPHAVANTAGE_DAILY_LIMIT` governs the guard: default `25`, and **`0` disables it entirely** (the "unlimited" sentinel for a paid plan). There is deliberately no `ALPHAVANTAGE_TIER` / `isAlphaVantagePaidTier()` — a tier vocabulary would be marketing surface with one internal consumer (BP-038); "set the limit to 0" both honors the issue's "no-op on a paid tier" and keeps the provider's public API to `hasAlphaVantageKey()` + fetchers + `_resetBudget`.

The check-and-reserve must be **atomic — a synchronous block with no `await` between the read and the increment** — because the desk fans analysts out concurrently; a check-then-increment-on-success design would let several parallel calls all pass `count < limit` before any increments (a TOCTOU race that burns past the cap). At the very top of `alphaVantageRequest`, before the `await fetch`:

```ts
// Parse defensively. The disable sentinel is the EXACT string "0" — nothing
// else. A positive integer sets the limit; everything else (blank, "twenty",
// "-5", "25.5", and the zero-like typos "0.0" / "00" / "-0") falls back to the
// 25 default rather than disabling the guard. (Two traps this closes: naïve
// `Number(env ?? 25)` makes Number("") === 0 and Number("typo") === NaN both
// read as ≤ 0 → guard off; and a `Number(raw) === 0` disable check would let
// "0.0"/"00"/"-0" silently switch protection off on a formatting mistake.)
// Require a configured key BEFORE reserving budget or fetching. A direct
// helper/fetcher call (a test, a script, a follow-on tool) with an empty/unset
// key would otherwise reserve a daily unit and fire a doomed request that only
// fails later via AV's HTTP-200 `Information` body — burning budget for nothing.
// Fail locally instead. (The tool closures already gate on hasAlphaVantageKey(),
// so this is the belt to that suspenders — it protects direct-call paths.)
if (!process.env.ALPHAVANTAGE_API_KEY?.trim()) {
  throw new AlphaVantageError("ALPHAVANTAGE_API_KEY is not set");
}

const raw = process.env.ALPHAVANTAGE_DAILY_LIMIT?.trim();
let limit: number;
if (raw === "0") {
  limit = 0;                                 // the explicit unlimited sentinel — exact match only
} else {
  const parsed = raw === undefined ? 25 : Number(raw);
  limit = Number.isInteger(parsed) && parsed > 0 ? parsed : 25;  // positive int, else default 25
}
if (limit > 0) {                             // 0 (only from raw === "0") → guard disabled
  const today = new Date().toISOString().slice(0, 10);   // UTC day
  if (budget.dayUtc !== today) { budget.dayUtc = today; budget.count = 0; }  // day roll
  if (budget.count >= limit) throw new AlphaVantageBudgetError(limit);
  budget.count += 1;                         // reserve synchronously — before any await
}
// ...then await fetch(...)
```

The parse is **fail-safe, not fail-open**: the only way to turn the guard off is to set the value to the exact string `"0"`. Everything else that *isn't* a clean positive integer — empty string, whitespace, `"twenty"`, `"-5"`, `"25.5"`, **and the zero-like typos `"0.0"` / `"00"` / `"-0"`** — resolves to the `25` default rather than to "disabled," so a formatting mistake near zero tightens protection instead of silently removing it.

The reservation is **not rolled back** on a downstream fetch failure. This is deliberate and cuts two ways in our favor: (a) it's race-free (the whole check-increment is synchronous); (b) **every attempted call counts — including one that comes back as a `Note`/`Information` throttle body** — so the counter never shows headroom after AV's server-side quota is actually spent (it errs toward stopping slightly early, never toward over-spending). A rare transport error that never reached AV costing one reserved unit is the acceptable trade. Callers catch `AlphaVantageBudgetError` like any other AV failure → degrade.

**Per-minute limit (5/min) is a real, separate constraint.** In normal dev the 5/min cap binds *before* 25/day: a single disclosure fan-out can fire the transcript probe + transcript (2) and the estimates enrichment (2) within seconds, and two back-to-back runs inside 60s will throttle on a later call via a `Note` body — degrading honestly, but with no proactive guard. v1 does **not** add a token bucket; it documents the burst pattern (README + §6) and uses `Promise.allSettled` for the independent enrichment calls so one throttle doesn't reject the other. A per-minute token-bucket / inter-call delay (via `lib/concurrency.ts`) is a named v2 follow-up, not required here.

**API key injection (required):** `alphaVantageRequest` appends `apikey=process.env.ALPHAVANTAGE_API_KEY` to every request — callers pass only the endpoint params (`function`, `symbol`, `quarter`, …). AV lists `apikey` as required for all query endpoints; a request that omits it comes back as an HTTP-200 `Information` body (which the helper then throws on), so a configured key that never reaches the URL would make every live branch degrade to `unavailable` despite the key being set. Centralizing the key here (not per-fetcher) means every AV fetcher — this issue's and all follow-ons' — inherits it. The `hasAlphaVantageKey()` gate stays the caller's cheap pre-check; the helper is the single place the key is actually attached. **The helper also requires the key locally before reserving budget or fetching** — a missing/blank key throws immediately (no unit reserved, no request sent), so a direct helper/fetcher call outside the key-gated tool closures (a test, a script, a follow-on tool) fails fast instead of burning a budget unit on a request AV would reject with an `Information` body. §7 test #1 covers this (missing key → throw, `fetch` not called, budget untouched).

**Body-error detection — distinguish quota throttles from request-shape errors:** after `JSON.parse`, if the object has a `Note` or `Information` key → `throw new AlphaVantageRateLimitError(body[key])` (a frequency/premium **quota** throttle — the quota is spent, don't retry). If it has an `Error Message` key → `throw new AlphaVantageRequestError(body[key])` (a **request-shape** error — an unrecognized param such as a bad quarter label, *not* a quota signal). Otherwise return the parsed object. The two error classes drive different fallback behavior: a `RateLimitError`/`BudgetError` degrades immediately (§4 transcript budget-block rule), while a **`RequestError` on the first transcript label is retry-eligible** — the alternate-label retry fires on it just as it does on an empty `transcript[]` (see the transcript flow above), so a first-label spelling miss recovers instead of collapsing to `unavailable`.

### Data flow — per tool

**`get_earnings_transcript`** (was: always `emptyPayload` in live mode):
```
resolveToolPayload → live closure:
  if (hasAlphaVantageKey()) { try { return await fetchAlphaVantageEarningsTranscript(input); } catch {} }
  return emptyPayload("get_earnings_transcript", input);
```
`fetchAlphaVantageEarningsTranscript`: **resolve the latest *reported* fiscal quarter as-of `input.date`, not the calendar quarter.** Calendar-quarter derivation is wrong for companies with offset fiscal years and for as-of dates that fall before the current quarter's call — it would request an unreported quarter and return `available: false` even when a recent transcript exists, and the disclosure prompt expects the *latest* call. So: (1) call `EARNINGS&symbol` → pick the most recent `quarterlyEarnings[]` entry whose `reportedDate` is on-or-before `input.date`; (2) call `EARNINGS_CALL_TRANSCRIPT&symbol&quarter` for that quarter.

**Quarter-label caveat (verify at implementation):** the transcript endpoint's `quarter` param uses AV's *fiscal-quarter* label (`YYYYQn`), and for issuers on a non-calendar fiscal year a naïve `fiscalDateEnding`→calendar-quarter mapping is wrong — e.g. Apple's FY2024 Q1 ends Dec 2023, so mapping the date gives `2023Q4` while AV expects `2024Q1`, returning empty even though the call exists. The `fiscalDateEnding` date alone doesn't carry the fiscal-year label. **Concrete derivation source — the same `EARNINGS` response, no extra AV call:** the `EARNINGS` payload carries an `annualEarnings[]` array whose `fiscalDateEnding` values reveal the issuer's **fiscal-year-end month** (e.g. Apple's annual `fiscalDateEnding` lands in September → FY ends September). Derive the fiscal-quarter label by counting the target quarter's `fiscalDateEnding` position within the fiscal year that FY-end month defines: the quarter ending 3 months after FY-end is Q1, and so on — so Apple's Dec-2023 `fiscalDateEnding` resolves to `2024Q1`, not `2023Q4`. This reuses data already in the probe response (no OVERVIEW call, no extra budget unit). **The first `EARNINGS_CALL_TRANSCRIPT` request MUST use this derived fiscal label** (not the calendar quarter) — so the common non-calendar case (Apple et al.) resolves in the happy-path 2 units, not 3. The implementer must still **confirm AV's exact expected label spelling against the live API during the record step**; and as a belt-and-suspenders robustness fallback, retry **once** with the alternate (calendar-quarter) label — but only when the first (fiscal-label) attempt **either returns an empty `transcript[]` or throws an `AlphaVantageRequestError`** (a request-shape rejection of the label, §4 body-error detection). A `RateLimitError`/`BudgetError` on the first attempt does **not** retry (the quota is spent — degrade immediately). Only after that single alternate-label attempt also comes up empty/rejected is the result `available: false`. **Probe-failure handling distinguishes budget errors from everything else:** if the `EARNINGS` probe throws an `AlphaVantageBudgetError` (or a rate-limit-body `AlphaVantageRateLimitError`), the quota is already spent — the transcript call would hit the same guard and be rejected too, so **degrade immediately without requesting a transcript** (a second doomed call, or worse, pressure to bypass the guard — inconsistent with §4/§6's "spent budget degrades without calling AV"). Only on a *non-budget* probe failure (network / non-2xx / JSON parse) fall back to the most-recent-completed calendar quarter as a best-effort transcript request. Map: `content` ← the `transcript[]` entries joined as `"{speaker} ({title}): {content}"`, capped to a prompt-safe length; `quarter` ← the resolved quarter; `callDate` ← the resolved `reportedDate` when available, else `null`; `available` ← `transcript.length > 0`; `source` ← `"alphavantage"`. **Budget note:** this is **two AV calls on the happy path (probe + transcript), or three when the empty-transcript alternate-label retry fires** (probe + first-label transcript + alternate-label transcript) — all pass through the budget guard, and the retry counts its own unit. Acceptable for once-per-run reference data; size the daily limit for the 3-unit worst case (see the budget accounting below).

**`get_analyst_estimates`** (was: Finnhub ratings + surprises; consensus/targets hard-null):
```
resolveToolPayload → live closure:
  fetch Finnhub ratings + surprises (as today);
  if (hasAlphaVantageKey()) {
    try { const enr = await fetchAlphaVantageAnalystEnrichment(input.ticker);
          merge enr.consensusEstimates, enr.priceTargets } catch {}   // partial: each field independent
  }
  // Provenance: PRIMARY-WINS. Keep source: "finnhub" whenever the Finnhub
  // baseline answered (ratings or surprises present) — a payload that is
  // mostly Finnhub with an AV price-target should not read as "alphavantage".
  // Tag "alphavantage" only when Finnhub is absent and AV filled something;
  // else emptyPayload.
```
`fetchAlphaVantageAnalystEnrichment`: **two independent `alphaVantageRequest` calls (so a `get_analyst_estimates` invocation costs 2 budget units, not 1)** issued via `Promise.allSettled` so one throttle doesn't reject the other — `OVERVIEW` → `priceTargets = { consensus: num(AnalystTargetPrice), high: null, low: null, median: null }` (AV gives a single target); `EARNINGS_ESTIMATES` → `consensusEstimates = { fyEpsAvg, fyRevenueAvg, numAnalysts }`. Each field is independent: OVERVIEW filling while EARNINGS_ESTIMATES rejects (throttle / budget) yields `priceTargets` set, `consensusEstimates` null. `recentRatingActions` stays `[]` (AV has no dated rating-action feed). **Budget accounting:** transcript = 2 units (probe + transcript), **or 3 when the non-calendar alternate-label retry fires**; estimates = 2 units (overview + estimates); insider = 1 — so a full disclosure fan-out spends ~5 AV units on the happy path, **up to ~6 when a transcript retry hits**; size `ALPHAVANTAGE_DAILY_LIMIT` for that worst case.

**`get_insider_transactions`** (was: Finnhub-only → empty):
```
resolveToolPayload → live closure:
  if (hasFinnhubKey()) { try { return await fetchFinnhubInsiderTransactions(input); } catch {} }
  if (hasAlphaVantageKey()) { try { return await fetchAlphaVantageInsiderTransactions(input); } catch {} }
  return emptyPayload("get_insider_transactions", input);
```
`fetchAlphaVantageInsiderTransactions`: `INSIDER_TRANSACTIONS&symbol&from=<90-day-start>`. AV returns the full transaction history by default, which is wasteful in this quota-conscious path — the AV docs list a server-side `from=YYYY-MM-DD` filter for this endpoint, so **pass the 90-day-window start (`input.date` − 90 days) to shrink the response** (confirm the exact param name against the live API during the record step; if it turns out unsupported, drop it and rely on the client-side filter alone). **Always keep the client-side upper-bound filter too** (`transactionDate <= input.date`, for back-dated as-of dates). Filter `data[]` to the 90-day window ending `input.date`, **then cap to the first 50 rows — `slice(0, 50)` — matching the Finnhub primary's cap (`finnhub.ts:397`) so a busy filer can't blow up the downstream prompt budget** (the AV fallback must preserve the same prompt-budget contract the primary honors). Map each capped row to `insiderTransactionItem`:
- `filingDate` ← `transaction_date` (AV has no separate filing date — documented approximation)
- `transactionDate` ← `transaction_date`
- `insiderName` ← `executive`; `insiderTitle` ← `executive_title`
- `transactionCode` ← **`""` (unknown).** AV's `acquisition_or_disposal` is only a direction flag (A/D), *not* the SEC transaction code the schema documents (P/S/A/M/G/F). Do **not** synthesize `P`/`S` from it — the news analyst prompt weighs open-market buys/sells against grants, option exercises, and tax-withholding sales, and a fabricated `P`/`S` would mislabel an award or a tax sale as an open-market trade. Leave the code unknown and carry *direction* in the sign of `shares` instead.
- `shares` ← signed: `+Number(shares)` for `A`, `-Number(shares)` for `D`
- `pricePerShare` ← `Number(share_price)`
- `isDerivative` ← concrete rule: `security_type` (case-insensitive) contains `"deriv"` **and not** `"non-deriv"` → `true`; otherwise `false`. (AV's `security_type` commonly reads `"Derivative"` / `"Non-Derivative"`; any other/blank value → `false`.) No open-ended heuristic — an explicit, testable predicate.
- `windowDays` ← `90`; `source` ← `"alphavantage"`.

The AV fallback is deliberately **coarser than the Finnhub primary** — direction + derivative flag, but no SEC-code granularity. That is the honest shape of AV's data, and the fallback is reached only when Finnhub (full fidelity) is unavailable.

### Runtime prompt updates (the memo reads these, not just the docs)

The two analyst prompts carry **provider-specific assumptions the generated memo repeats**, so leaving them stale would make the memo misattribute coverage or over-read the coarser AV insider rows in a live AV run. Both are runtime artifacts in this diff's scope (not just README/guide docs):

- **`disclosure.prompt.md`** — today it hardcodes FMP as the transcript/estimates provider: the "Earnings call" section degrades to *"transcript unavailable — no FMP key or no transcript for this quarter"*, "Consensus & revisions" calls `consensusEstimates`/`priceTargets`/`recentRatingActions` *"FMP-only fields"*, and the "Data coverage" + degraded-data rules say *"no FMP key"*. With AV now backing transcripts and enriching estimates, rewrite these to be **provider-agnostic** — degrade on *"no transcript for this quarter"* / *"estimates unavailable"* without naming FMP (the memo shouldn't blame a missing FMP key when AV is the actual provider, or claim FMP-exclusivity for fields AV now fills). `recentRatingActions` stays honestly `[]` on the AV path (AV has no dated rating-action feed), so keep its "degrades when null" wording.
- **`news.prompt.md`** — its `insiderTransactions` description promises a populated *"transaction code"*, and the weighing clause tells the analyst to distinguish *"derivative-only activity (option exercises, RSU vests) vs. open-market trades"* — a P/S/A/M read that only the Finnhub primary supports. On the AV fallback `transactionCode` is `""`, so add one clause: when the transaction code is blank (AV-sourced), lean on **direction (the sign of `shares`) and the derivative flag**, and do **not** infer open-market-buy/sell semantics from an absent code. This keeps the memo from reading a coarse AV row as if it carried SEC-code granularity.

These are prompt-copy edits only — no schema or output-shape change. Their verification is the real-provider memo read (§7 goal check), not a unit test.

### State management

None. The provider module holds one process-level budget counter (module scope, not session state). No new session state, resources, or scopes. The counter is orthogonal to the FIX-758 session spine (per-session) and the `lib/cache.ts` process cache (120s TTL) — it neither reads nor writes either.

### Error handling

| Failure | Behavior |
| --- | --- |
| No `ALPHAVANTAGE_API_KEY` | AV branch skipped; tool degrades to the next fallback / `emptyPayload`. |
| AV `Note`/`Information` body (HTTP 200) — quota throttle | `alphaVantageRequest` throws `AlphaVantageRateLimitError` → tool catches → degrade (quota spent; no retry). |
| AV `Error Message` body (HTTP 200) — request-shape error | `alphaVantageRequest` throws `AlphaVantageRequestError` (distinct class) → tool catches → degrade; on the **first transcript label** it is retry-eligible (alternate-label retry, §4). |
| Daily budget spent | `alphaVantageRequest` throws `AlphaVantageBudgetError` **before** fetching → degrade. Guard disabled **only** when `ALPHAVANTAGE_DAILY_LIMIT` is exactly `0`. |
| Malformed `ALPHAVANTAGE_DAILY_LIMIT` (blank / negative / non-integer / non-numeric) | Fail-safe: value is ignored and the guard falls back to the `25` default — a misconfiguration never silently disables protection. |
| Non-2xx / network / JSON parse | Fetcher throws → tool catches → degrade. |
| Partial (one AV call succeeds, another fails) — analyst-estimates | `Promise.allSettled` per field; the succeeding field fills, the failing one stays `null`. |
| Empty transcript for the resolved quarter | `available: false`, `content: null` — a valid "no transcript" result, `source: "alphavantage"`. |

All AV failures are non-retryable at the tool layer and collapse to degrade-to-next. No retry/backoff inside the fetchers (a throttled AV key won't recover within a run; retrying burns budget).

## 5. Implementation Sequence

Discipline: **`fsd:tdd`** (Feature label) — red-green-refactor, tracer-bullet per behavior.

1. **`sourceTag` + provider skeleton** — add `"alphavantage"` to the `sourceTag` enum; create `alpha-vantage.ts` with `hasAlphaVantageKey`, the error classes, and `alphaVantageRequest` (**apikey injection**, **atomic synchronous budget check-and-reserve before `await fetch`**, single `ALPHAVANTAGE_DAILY_LIMIT` knob with `0`=disabled + Note/Information/Error-Message detection + `_resetBudget`). *Verify:* `test/alpha-vantage.spec.ts` — helper injects `apikey` into the request URL, throws on a `{Note}` body, throws `AlphaVantageBudgetError` at the limit **and under concurrent calls (N parallel requests never exceed the limit)**, `limit=0` disables the guard, reserves on entry. `pnpm --filter @flow-state-dev/trading-desk typecheck`.
2. **Insider fallback** — add `fetchAlphaVantageInsiderTransactions`; wire it as the terminal fallback in `get_insider_transactions.ts`; **add the blank-`transactionCode` clause to `news.prompt.md`** (direction + derivative flag when the code is absent). *Verify:* extend `test/get-insider-transactions.spec.ts` — Finnhub-fails → AV-answers path tags `alphavantage`, **the fetch URL carries `from=<input.date − 90 days>`**, the client-side 90-day window drops an older row, A/D sign mapping correct, `transactionCode` left `""` (not fabricated). Depends on: step 1.
3. **Transcript stub** — add `fetchAlphaVantageEarningsTranscript` (**with the `EARNINGS`-probe latest-reported-quarter resolver**, calendar-quarter fallback on non-budget probe failure only); rewrite `get_earnings_transcript.ts` live path and **correct its stale FMP surface — the "PR2 wires FMP" header comment AND the model-facing handler `description` string** (currently "FMP_API_KEY (free tier)…"), so the tool metadata the analyst reads names AV, not a dead provider; **update the "Earnings call" degradation wording in `disclosure.prompt.md`** to drop "no FMP key". *Verify:* `test/get-earnings-transcript.spec.ts` — `EARNINGS` probe picks the latest reported quarter, transcript for it → `available: true`, content joined; empty transcript → `available: false`; non-budget probe failure → calendar-quarter fallback; budget-blocked probe → degrade without a transcript call. Depends on: step 1.
4. **Estimates enrichment + disclosure prompt** — add `fetchAlphaVantageAnalystEnrichment`; extend `get_analyst_estimates.ts` to merge AV consensus/targets and **correct its stale FMP surface — the "wired in PR2" header comment AND the model-facing handler `description` string** (currently "…(when FMP key is set) forward consensus estimates, price targets…") so it names AV; **de-FMP-ify `disclosure.prompt.md`** (provider-agnostic transcript/estimates degradation, drop "FMP-only fields" — this step owns the estimates half, the transcript wording lands with step 3). *Verify:* `test/get-analyst-estimates.spec.ts` — Finnhub baseline + AV enrichment fills `consensusEstimates`/`priceTargets` while **`source` stays `"finnhub"` (primary-wins)**; partial (OVERVIEW-only) leaves `consensusEstimates` null; Finnhub-absent + AV-present tags `"alphavantage"`. Depends on: step 1.
5. **Fixtures + record seam** — generate/record AV-backed fixtures for the three tools under the existing per-ticker fixture dirs (record mode with a real key, or hand-curated to schema). *Verify:* fixture-mode tests load them. Depends on: 2–4.
6. **Docs** — README provider-keys + env (`ALPHAVANTAGE_API_KEY`, `ALPHAVANTAGE_DAILY_LIMIT` incl. `0`=unlimited) + a per-minute/burst note + the serverless cold-start caveat + three tool-inventory bullets; CLAUDE.md provider list; walkthrough provider-keys paragraph. **REPLACE (don't just append) every stale `FMP_API_KEY`-enables-transcripts claim** — the README tool-inventory bullet, the README "Provider keys" sentence, and the walkthrough provider-keys paragraph — so published setup docs never tell users to configure a key this spec leaves dead. *Verify:* the key + budget behavior are documented; **no README/walkthrough line still says `FMP_API_KEY` enables/requires earnings-call transcripts** (grep `FMP` in both files returns nothing user-facing).

Steps 2/3/4 are independent after step 1; 5 needs 2–4; 6 last.

## 6. Edge Cases & Error Handling

The provider-failure → degrade paths (no key, `Note`/`Information` body, budget spent, non-2xx/parse) are the §4 error table; this section covers only cases §4 doesn't:

| Case | Expected behavior |
| --- | --- |
| **Per-minute throttle (5/min) before the daily cap** | The likelier binding limit in dev — a disclosure fan-out fires transcript-probe+transcript (2) + estimates (2) in seconds; back-to-back runs within 60s throttle on a later call via a `Note` body → honest degrade. No proactive per-minute guard in v1 (documented; token-bucket is v2). |
| Concurrent calls near the daily limit | The synchronous check-and-reserve (no `await` between read and `count += 1`) makes the guard race-free: N parallel `alphaVantageRequest`s never collectively exceed the limit; every attempted call (incl. throttled ones) counts. |
| Process restart / serverless cold-start resets the counter | Per-process limitation — on Vercel every invocation may start fresh, so the daily guard is best-effort, not a hard cross-run 25/day circuit breaker. AV's server-side throttle (→ Note body → throw) is the real-exhaustion backstop. |
| `ALPHAVANTAGE_DAILY_LIMIT` override | Default 25; lower for testing; **only the exact string `"0"` disables the guard** (paid / metered plan). A malformed value — blank / negative / non-integer / non-numeric, **including zero-like typos `"0.0"` / `"00"` / `"-0"`** — falls back to the 25 default. |
| Insider row with unparseable `shares`/`share_price` | That row's numeric coerces via a null-safe helper; a fully unparseable row is dropped (ticker-level data, not a nullable field, so a bad row can't stay). |
| Transcript: non-calendar fiscal year | The `EARNINGS` probe + fiscal-label resolution (§4) targets the latest *reported* quarter; if the first label yields empty, retry with the alternate label before `available: false`. **The retry is a third guarded AV call** (probe + 2 transcript attempts = 3 units), so the transcript worst case is 3, not 2. |
| Insider row from AV (fallback) | `transactionCode` is `""` (unknown — AV lacks the SEC code); direction carried by the sign of `shares`. Coarser than the Finnhub primary by design. |
| AV `AnalystTargetPrice` is `"None"`/`"-"` | `num()` → `null`; `priceTargets.consensus` null, rest of the estimates payload intact. |
| Finnhub answers insider, AV never called | AV fallback is terminal — reached only when Finnhub throws/has no key. No extra budget spent on the happy path. |

Error taxonomy: all AV-layer failures (rate-limit body, budget, network, parse) are non-retryable and collapse to degrade-to-next-or-empty. The tools never throw.

## 7. Testing Strategy

**Goal & goal check (real provider).** *Goal:* with a real `ALPHAVANTAGE_API_KEY`, the two previously-dead stub tools return real live data and the insider tool has a working second source — without silently burning the 25/day quota. *Goal check (out-of-CI, real key):* a record-mode run (or a small `scripts/record-alpha-vantage.ts`) against a real key that fetches transcript + estimates + insider for one ticker. **Assert per the provenance rules, not a blanket `source: "alphavantage"`:** transcript has no competing primary, so it returns `source: "alphavantage"` with a populated `content`. Estimates (primary-wins) and insider (terminal fallback) stay `source: "finnhub"` whenever a `FINNHUB_API_KEY` is present, but the two are **not** checkable the same way:

- **Estimates** — AV *enriches* the Finnhub baseline, so its fields appear even with Finnhub keyed. Keep Finnhub and assert the **AV-contributed fields** directly (`consensusEstimates`/`priceTargets` non-null), not the top-level `source`.
- **Insider** — AV is a **terminal fallback**, reached only after `fetchFinnhubInsiderTransactions` throws or has no key. With Finnhub keyed, a normal run returns Finnhub rows and never touches AV, so a field assertion would prove nothing. The insider check must **unset/force-fail `FINNHUB_API_KEY`** (then the tool tags `"alphavantage"` and the AV-shaped rows — `transactionCode === ""` — appear), **or** exercise `fetchAlphaVantageInsiderTransactions` directly at the helper level, bypassing the chain.

Alternatively, unset Finnhub for the whole run so all three tools exercise the AV path and tag `"alphavantage"`. A blanket `source: "alphavantage"` assertion *with Finnhub keyed* would either fail the check or wrongly pressure the implementation to mis-tag mixed payloads. A second assertion drives `alphaVantageRequest` past the configured limit and confirms it throws `AlphaVantageBudgetError` rather than calling AV.

**Two facets, two checks — the data path is model-free, the prompt edits are NOT.** The provider/tool plumbing has no LLM (the record-run assertions above are the real-path evidence, BP-003, and double as the step-5 fixture generator). But this diff also edits the **runtime disclosure/news prompts + tool `description` strings** (§4 Runtime prompt updates), and a data-only record script would pass while the generated memo still repeats stale FMP or insider-code assumptions. So the goal check **also runs a headless memo read-back** (`fsd:verify-trading-desk` — a real `fsdev run analyze` + the zero-model `runSummary`/memo read) on a ticker whose AV path is exercised, and asserts the disclosure memo no longer says "no FMP key" / attributes transcripts+estimates to FMP, and the news memo reads AV insider rows by direction + derivative flag rather than a (blank) transaction code. That memo read is the prompt-edit verification gate; without it the "No LLM involved" framing would leave the prompt changes unverified.

**CI specs** (`fsd:tdd`, vitest, deterministic — `testBlock` per AGENTS.md rule 4, mock `globalThis.fetch`, `_resetBudget()` + `_resetCache()` in `beforeEach`, `chdir` to app root):

Behaviors to test (each a tracer-bullet cycle):
1. **Request helper — key gate + apikey + body errors (quota vs request-shape)** — with `ALPHAVANTAGE_API_KEY` unset, `alphaVantageRequest` **throws locally before any reserve or fetch** (`fetch` not called, budget counter unchanged); with a key set, a successful call asserts the outgoing `fetch` URL carries `apikey=<ALPHAVANTAGE_API_KEY>` (injected by the helper, not the caller); a mocked `fetch` returning HTTP 200 `{ "Note": "...frequency..." }` → `alphaVantageRequest` throws `AlphaVantageRateLimitError`; same for `{ Information }`; **`{ "Error Message": "..." }` throws the distinct `AlphaVantageRequestError`** (request-shape, not quota).
2. **Budget guard (incl. concurrency)** — with limit 2 and free tier: 3rd `alphaVantageRequest` throws `AlphaVantageBudgetError` without calling `fetch`; **5 `alphaVantageRequest`s fired in parallel and collected with `Promise.allSettled` (NOT `Promise.all` — the budget rejections would short-circuit it before the invariant is checked) against a slow-`fetch` mock settle to exactly 2 fulfilled fetches + 3 rejected `AlphaVantageBudgetError`s** (proves the reserve-before-await is race-free); with `ALPHAVANTAGE_DAILY_LIMIT=0` the guard is disabled (the 3rd call proceeds); a **malformed limit (`""`, `"  "`, `"-5"`, `"25.5"`, `"twenty"`, and the zero-like typos `"0.0"`, `"00"`, `"-0"`) falls back to the 25 default — the guard stays ON**, not silently disabled (only the exact string `"0"` disables it); UTC-day roll resets the counter.
3. **Insider fallback** — Finnhub mocked to throw, AV mocked to return the `data[]` shape → `source: "alphavantage"`, `A`→positive shares / `D`→negative, `transactionCode` is `""` (not fabricated to `P`/`S`). **Assert both window filters plus the row cap:** (a) the outgoing `fetch` URL carries `from=<input.date − 90 days>` (the server-side filter is actually sent, not just declared — a quota-conscious fallback that omits it would silently download full histories), (b) a mocked row *older* than the 90-day window is dropped by the client-side filter, and (c) **a mocked `data[]` of 60 in-window rows yields exactly 50 mapped transactions** (the `slice(0, 50)` prompt-budget cap, matching the Finnhub primary).
4. **Transcript** — `EARNINGS` mocked → picks the latest reported quarter; `EARNINGS_CALL_TRANSCRIPT` mocked with a `transcript[]` → `available: true`, `content` joined and capped, `quarter` + `callDate` set; empty `transcript[]` → `available: false`, `content: null`; `EARNINGS` probe fails with a **non-budget** error (network/parse) → calendar-quarter fallback path taken; `EARNINGS` probe fails with `AlphaVantageBudgetError` → **degrade immediately, `fetch` not called a second time for a transcript** (no doomed retry against a spent quota). **Non-calendar fiscal year (regression against a naïve calendar mapper — and against a lazy always-retry impl):** an issuer whose latest `fiscalDateEnding` is Dec but whose `annualEarnings[]` shows a Sep fiscal-year-end → derived fiscal label `YYYYQ1`, not the calendar `YYYYQ4`; the transcript-mock keys on the **fiscal** label. **Assert the FIRST `EARNINGS_CALL_TRANSCRIPT` request URL carries the derived fiscal label** (`2024Q1`) and that it reaches `available: true` in **exactly 2 units (probe + one transcript call — NO retry)** — this fails both a calendar-only mapper (wrong label) and a lazy "calendar-first-then-retry" impl (which would spend 3 units on every Apple-like lookup). **Retry path (separate case):** the first fiscal-label attempt returns empty `transcript[]` **or** throws `AlphaVantageRequestError` (a request-shape label rejection) → the fetcher retries once with the alternate label and, on success, spends a third unit (probe + 2 transcript attempts = 3) — the budget accounting's 3-unit worst case; a first-attempt `AlphaVantageRateLimitError`/`BudgetError` does **not** retry.
5. **Estimates enrichment (primary-wins provenance)** — Finnhub baseline present, AV `OVERVIEW`+`EARNINGS_ESTIMATES` mocked → `consensusEstimates` + `priceTargets` filled but **`source` stays `"finnhub"`** (the Finnhub ratings/surprises baseline answered — §4 primary-wins); OVERVIEW-only (EARNINGS_ESTIMATES throws) → `priceTargets` filled, `consensusEstimates` null, `source` still `"finnhub"`. Separately, **Finnhub absent (ratings + surprises both throw) + AV enrichment present → `source: "alphavantage"`** (AV is the only contributor); Finnhub absent + AV absent → `emptyPayload`.
6. **No-key degrade** — `ALPHAVANTAGE_API_KEY` unset → AV branch skipped, `fetch` not called for AV, tool degrades exactly as today.

Reference patterns: `test/finnhub-insider.spec.ts` / `test/finnhub-fundamentals.spec.ts` (provider-fetch mock idiom), `test/get-insider-transactions.spec.ts` (tool degrade paths), `test/get-futures-curve.spec.ts` (single-provider live/degrade), `fsd:write-block-tests` for the mock-context idiom.

## 8. Non-Goals

- **AV as a primary or preferred provider for any signal** — FIX-675 (composition strategy + data-quality bake-off) owns that decision. AV stays terminal fallback + stub-completer here.
- **Consuming or building the availability-aware resolver** — FIX-805. This uses the existing per-tool `try/catch` convention; FIX-805 later refactors all chains.
- **Cross-run / persistent daily-budget accounting** — the guard is process-scoped by design. Persistent budget + budget-aware routing is FIX-675 / procurement territory.
- **Threading `AbortSignal`/`ctx.signal` into the AV fetches** — deliberately not added, for three reasons. (1) *Convention:* **no** desk provider threads cancellation today — not Finnhub (the closest template), not the tool runtime (`runtime/resolve.ts`), and the shared `getOrFetch` cache wrapper (`lib/cache.ts`) every data fetch funnels through has no signal seam. Adding it to AV alone would be an inconsistent one-off (BP-038), and doing it properly means changing the shared cache/runtime layer — a framework-wide cancellation-threading change that owns its own issue (BP-028), not this fallback-coverage diff. (2) *Budget is already safe:* the guard **reserves its unit synchronously before the `await fetch`** (the race-free design, §4), so a cancelled in-flight AV call has *already* spent its reserved unit — aborting the socket would not reclaim budget. The only cost of not aborting is a dangling network request, which every other desk provider also incurs. (3) *Scope:* if desk-wide fetch cancellation is wanted, it should land uniformly across all providers via the shared fetch/cache layer, not be bolted onto this one fallback.
- **AV fallback for `get_macro_indicators`** — FIX-802 (AV covers only ~4 of 9 series, budget-expensive for a partial fill).
- **AV for `get_social_sentiment` / ticker news sentiment** — FIX-799 (`NEWS_SENTIMENT` is a news-article modality, distinct from `get_social_sentiment`'s X/Twitter chatter).
- **The richer AV category tools** — news sentiment (FIX-799), top movers (FIX-800), ETF profile (FIX-801), macro/commodities (FIX-802), server-side technical indicators (FIX-803), forward corporate events (FIX-804). All depend on this module.
- **Realtime options/index, institutional/13F, crypto pricing** — per the issue's explicit deferrals (premium-gated / not offered by AV / FIX-773 respectively).
- **Removing the dead `fmp.ts` stub or migrating to FMP** — `lib/providers/fmp.ts` (a `hasFmpKey()`-only stub) is pre-existing dead code, left in place (surgical-changes: don't delete dead code you didn't create). **But do correct the now-actively-false FMP references this diff touches** — in the two rewritten tool files, both the header comments (`get_earnings_transcript.ts` "PR2 wires FMP" / `get_analyst_estimates.ts` "FMP enrichment … wired in PR2") **and the model-facing handler `description` strings** (`get_earnings_transcript.ts` "FMP_API_KEY (free tier)…" / `get_analyst_estimates.ts` "…(when FMP key is set) forward consensus estimates, price targets…") — the analyst reads the tool `description`, so an AV-backed run must not advertise a dead provider; the FMP degradation copy in `disclosure.prompt.md` (§4 Runtime prompt updates); and the stale `FMP_API_KEY`-enables-transcripts wording in the README + walkthrough setup docs (§9.3). All corrected to AV (FIX-798) since FMP no longer wires any live tool. Leaving a "coming soon: FMP" / "requires FMP_API_KEY" breadcrumb after this ships would misdirect the next reader (or tell a user to configure a dead key). Flag `fmp.ts` **and the now-permanently-unreachable `"fmp"` `sourceTag` enum value** as known-dead for a future cleanup pass (out of this diff's ownership).

*Deepening opportunities observed (not in scope):* the ~15 `data/get_*` tools each inline a bespoke `try/catch` provider chain — the exact duplication FIX-805 exists to collapse. Not addressed here; flagged as the standing follow-up via `fsd:improve-codebase-architecture` once FIX-805 lands.

## 9. Documentation Plan

**9.1. Docs change required?** Yes — a new configurable provider (`ALPHAVANTAGE_API_KEY` + `ALPHAVANTAGE_DAILY_LIMIT`), two tools that change from dead-in-live to working, and a new provenance source users will see in transcripts.

**9.2. Surfaces affected:**
- [ ] Reference docs (`apps/docs/docs/`) — No; the desk is a lab example, not a framework concept with an API page.
- [x] Guides (`apps/docs/guides/`) — `apps/docs/guides/trading-desk-walkthrough.md` (the provider-keys paragraph, ~line 233, is the canonical live-run key list; AV now backs the disclosure analyst's transcript + estimates and the news analyst's insider fallback, so the key belongs there). This is the FIX-800-flagged walkthrough update FIX-798 owns.
- [x] Package README — `labs/trading-desk/README.md` (provider-keys section + three tool-inventory bullets).
- [x] Other in-repo docs — `labs/trading-desk/CLAUDE.md` (provider list, if enumerated).
- [ ] Architecture docs (`docs/architecture/`) — No; no framework contract changes.
- [ ] Blog — No.

**9.3. Per-page plan:**

```
File: labs/trading-desk/README.md
Action: EXTEND + REPLACE (the FMP wording is stale — this spec leaves FMP dead)
Section: "## Provider keys" (~line 540) + the Phase 1 tool-inventory bullets (~78).
  - Provider keys: add ALPHAVANTAGE_API_KEY (optional, free tier 25 req/day,
    5 req/min) — enables live earnings-call transcripts + analyst
    consensus/price-targets and an insider-transactions fallback. Document
    ALPHAVANTAGE_DAILY_LIMIT (default 25; 0 = unlimited, for a paid plan). Two
    sentences on the guard's shape: (1) once the day's budget is spent, AV-backed
    tools degrade to unavailable; (2) it is an in-process/best-effort guard (a
    process restart or serverless cold-start resets it), and the 5/min per-minute
    limit — not the daily cap — is what binds when runs are close together.
  - REPLACE the stale FMP sentence in "## Provider keys" (~line 540: "FMP_API_KEY
    (optional, free tier) enables earnings-call transcripts via
    get_earnings_transcript; without it the disclosure analyst still runs on EDGAR
    filings and Finnhub ratings alone") — transcripts now come from AV, not FMP.
    Do NOT leave both keys claiming to enable transcripts; the ALPHAVANTAGE_API_KEY
    sentence supersedes it, and FMP_API_KEY is no longer a documented key (it's
    dead — see §8). Same for the tool-inventory bullet (~line 78: "get_earnings_transcript
    requires FMP_API_KEY (free tier, optional)") → get_earnings_transcript uses
    Alpha Vantage; get_analyst_estimates gains AV consensus + price targets on top
    of the Finnhub baseline; get_insider_transactions falls back to AV when Finnhub
    is absent. Leaving a "requires FMP_API_KEY" line would tell users to configure a
    key that can't make the live tool work.
Voice notes: terse inventory style; minimal em-dashes; don't call it "powerful";
  say plainly AV is a fallback/stub-completer, not the primary source.

File: apps/docs/guides/trading-desk-walkthrough.md
Action: REPLACE (not append — the existing FMP sentence is now false)
Section: the provider-keys paragraph (~line 239).
  - REPLACE the stale sentence "FMP_API_KEY is optional for earnings-call
    transcripts; without it, the disclosure analyst still runs on EDGAR filings
    (keyless) and Finnhub ratings (existing key)." with an AV sentence:
    ALPHAVANTAGE_API_KEY is optional and enables live earnings-call transcripts,
    analyst consensus/price-targets, and an insider-transactions fallback; free
    tier is budget-limited (25/day) and degrades to unavailable when spent
    (the disclosure analyst still runs on EDGAR filings + Finnhub ratings without
    it). FMP_API_KEY must NOT survive in the paragraph — an add-only edit would
    tell users to configure a key that no longer wires any live tool.
Audience: a developer setting up the desk for a live run. Match the paragraph's
  existing "KEY is optional for X; without it Y returns unavailable" cadence.
Voice notes: this is published site prose — no internal issue IDs; minimal
  em-dashes; introduce nothing without a plain-language gloss.

File: labs/trading-desk/CLAUDE.md
Action: EXTEND
Section: wherever providers/tools are enumerated (if present).
  - Add alpha-vantage to the provider list with its fallback/stub-completer role
    and the budget-guard note.
```

**9.4. Sidebar diff summary:** none — no `apps/docs` sidebar changes (extending an existing guide, not adding a page).

**9.5. Cross-link audit:** none — no new pages.

**9.6. Docs non-goals:** no new `apps/docs` reference page or guide; no per-endpoint AV API doc (the follow-on category issues document their own tools); no `.env.example` change (the README is the key-doc home — provider keys aren't currently in `.env.example`).

## 10. Dependencies

- **None blocking.** FIX-758 (session spine) is Done but not required (per-session, orthogonal to the process budget counter). FIX-805 (resolver) and FIX-675 (composition/bake-off) are Backlog but explicitly **not** consumed — this issue is deliberately independent of both.
- **Blocks:** FIX-800 (movers — already `blockedBy` this) and the rest of the AV family (FIX-799/801/802/803/804) which build on this provider module + budget guard + `alphavantage` sourceTag.
- **External:** an Alpha Vantage API key (free tier suffices; 25 req/day) for the record step / real-provider goal check.

## 11. Open Questions

**Confirmed (proceed unless overridden):**

- **Fallback list = insider + two stubs only**; macro→FIX-802, social/news→FIX-799. *(Owner-confirmed.)*
- **In-process budget guard, no DB; one `ALPHAVANTAGE_DAILY_LIMIT` knob (0 = disabled).** *(Override: DB-backed cross-run counter — but that pulls the DB into the provider layer and overlaps FIX-675; recommended only if free-tier exhaustion across runs proves a real problem in practice.)*
- **Complete stubs via AV, leave `fmp.ts` untouched.** *(Override: wire FMP instead/also — rejected as standing up a second provider for no added coverage.)*

**Resolved during spec review (were open, now decided):**

- **Insider `transactionCode` fidelity — RESOLVED: leave it unknown (`""`), don't fabricate `P`/`S`.** AV gives only A/D (direction), not the SEC code; synthesizing `P`/`S` would mislabel awards/exercises/tax-sales as open-market trades and skew the news analyst's derivative-vs-open-market weighing. Direction is carried by the sign of `shares`; the AV fallback is honestly coarser than the Finnhub primary (§4).
- **Transcript quarter — RESOLVED: probe the latest *reported* quarter via `EARNINGS`, don't derive the calendar quarter.** Calendar derivation breaks on offset fiscal years and back-dated as-of dates and would return `available: false` when a recent call exists; the prompt expects the latest call. Two AV calls (probe + transcript) is acceptable for once-per-run reference data; probe failure degrades to a calendar-quarter best-effort (§4; regression-tested against a naïve calendar mapper in §7 test #4). Probe failure distinguishes budget errors (degrade immediately, no transcript call) from non-budget failures (calendar-quarter best-effort).
- **Budget guard concurrency — RESOLVED: atomic synchronous check-and-reserve before `await fetch`.** The original check-then-increment-on-success design had a TOCTOU race under the analyst fan-out; reserving synchronously closes it (§4, tested in §7).
- **Budget-limit env validation — RESOLVED: fail-safe parse, only a literal `0` disables the guard.** A naïve `Number(process.env.ALPHAVANTAGE_DAILY_LIMIT ?? 25)` is fail-open — `Number("")` is `0` and `Number("typo")` is `NaN`, both making `limit > 0` false and silently disabling protection on a misconfiguration. The guard now trims and treats **only the exact string `"0"`** as the disable sentinel — a positive integer sets the limit, and everything else (blank, non-numeric, negative, non-integer, **and the zero-like typos `"0.0"` / `"00"` / `"-0"`**) falls back to the `25` default, so a typo (even a near-zero one) tightens protection instead of removing it (§4, §6 error table, §7 test #2).
- **API key injection — RESOLVED: the shared helper attaches `apikey`.** The helper contract initially took arbitrary params without stating the key was appended; AV requires `apikey` on all query endpoints, and omitting it returns an HTTP-200 `Information` body that degrades every live branch despite a configured key. `alphaVantageRequest` now injects `apikey=ALPHAVANTAGE_API_KEY`; §7 test #1 asserts the key is in the request URL (§4).
- **Transcript retry after budget rejection — RESOLVED: don't.** The probe-failure fallback originally lumped budget-blocked probes with network/parse failures, which would fire a doomed calendar-quarter transcript call against a spent quota (or pressure bypassing the guard). Budget/rate-limit probe errors now degrade immediately without a transcript request; only non-budget failures take the calendar best-effort (§4, §6, §7 test #4).
- **Insider real-provider goal check — RESOLVED: force Finnhub off (or test at the helper level).** Insider is a terminal fallback, so a field assertion with Finnhub keyed never exercises the AV path (Finnhub answers first). The goal check unsets/force-fails `FINNHUB_API_KEY` for the insider case, or drives `fetchAlphaVantageInsiderTransactions` directly (§7).
- **Runtime analyst prompts carry provider assumptions — RESOLVED: edit them in this diff.** `disclosure.prompt.md` hardcoded FMP ("no FMP key" degradation, "FMP-only fields") and `news.prompt.md` assumed a populated `transactionCode`; in a live AV run the memo would misattribute coverage or over-read the coarser AV insider rows. Both prompts are edited alongside the tool wiring — provider-agnostic degradation in disclosure, a blank-code direction-only clause in news — added to §4 (Runtime prompt updates), the §4 file list, and the §5 sequence. Prompt-copy only, no schema change; verified via the real-provider memo read (§7).
- **Stale FMP setup docs — RESOLVED: replace, don't append.** The README (tool-inventory bullet + "Provider keys" sentence) and the walkthrough provider-keys paragraph each still claim `FMP_API_KEY` enables/requires earnings-call transcripts. Since this spec leaves FMP dead and moves transcripts/estimates to AV, an add-only docs edit would tell users to configure a key that can't wire any live tool. §9.3 + §5 step 6 now specify **replacing** the FMP wording with the AV wording in all three places (grep `FMP` in both files returns nothing user-facing).
- **Model-facing tool `description` strings still advertised FMP — RESOLVED: update them too.** `get_earnings_transcript.ts` ("FMP_API_KEY (free tier)…") and `get_analyst_estimates.ts` ("…(when FMP key is set) forward consensus estimates, price targets…") carry `description` metadata the analyst reads at tool-selection/interpretation time (a real model-facing surface, not just a code comment). An AV-backed run would keep telling the analyst about a dead provider unless these are edited, so §5 steps 3/4 and §8 now cover the `description` strings alongside the header comments.
- **Budget-concurrency test must use `Promise.allSettled` — RESOLVED.** §7 test #2's "5 parallel requests → 2 fetches + 3 `AlphaVantageBudgetError`s" invariant can't be observed with `Promise.all` (it rejects on the first budget error before the assertion). The test wording now specifies `Promise.allSettled` (or per-promise catches) so all five outcomes are collected.
- **Transcript alternate-label retry costs a budget unit — RESOLVED: account for it as 3.** The budget accounting said transcript = 2 units, but §4's empty-transcript alternate-label retry (the non-calendar-issuer robustness path) fires a third `EARNINGS_CALL_TRANSCRIPT` call, so the transcript worst case is 3 units and a disclosure fan-out can spend ~6, not ~5. Rather than drop the retry (it fixes a real correctness bug), §4/§6 now document the 3-unit worst case and §7 test #4 asserts the retry spends the third guarded call — so sizing `ALPHAVANTAGE_DAILY_LIMIT` and the real-provider budget check are honest.
- **AV insider rows must carry the 50-row cap — RESOLVED.** The Finnhub primary caps `get_insider_transactions` at `slice(0, 50)` (`finnhub.ts:397`) so a busy filer can't blow up the downstream prompt budget; the AV fallback mapped the whole filtered `data[]` and would break that contract. §4 now caps the AV fetcher at the first 50 in-window rows and §7 test #3 asserts a 60-row mock yields exactly 50 — the fallback preserves the primary's prompt-budget contract.
- **Prompt edits need a memo-read gate, not just a data record — RESOLVED.** The goal check said "No LLM is involved," but this diff also edits the runtime disclosure/news prompts + tool `description` strings, and a data-only record script would pass while the memo still repeats stale FMP/insider-code assumptions (§4 already said those edits are verified by a real-provider memo read). §7 now splits the check into two facets — the model-free data path (record-run assertions) **and** a headless `fsd:verify-trading-desk` memo read-back that asserts the disclosure memo drops FMP and the news memo reads AV insider rows by direction/derivative flag, not a blank code.
- **Cancellation / `AbortSignal` threading — RESOLVED: out of scope (declined).** No desk provider threads `ctx.signal` today (not Finnhub, not the tool runtime, and the shared `getOrFetch` cache wrapper has no signal seam), so adding it to AV alone is an inconsistent one-off (BP-038); a proper desk-wide fetch-cancellation change belongs to its own issue via the shared cache/runtime layer (BP-028). Moreover the budget unit is reserved *before* the `await fetch`, so a cancelled in-flight call has already spent its unit — aborting reclaims no budget. Documented as a §8 non-goal.
- **Helper must fail on a missing key before reserving budget — RESOLVED.** `alphaVantageRequest` injects `apikey` but a direct helper/fetcher call (test, script, follow-on tool) with an empty/unset key would reserve a daily unit and fire a doomed request that only fails later via AV's `Information` body. The helper now **requires + trims the key at the very top — throwing before any reserve or fetch** — so a keyless direct call fails locally and burns no budget (§4 budget snippet + key-injection prose; §7 test #1).
- **Fiscal-label derivation source — RESOLVED: derive from the `EARNINGS` response's `annualEarnings[]`, no extra call.** The caveat noted `fiscalDateEnding` alone doesn't carry the fiscal-year label, leaving the `YYYYQn` label unsourced. §4 now names a concrete zero-extra-cost source: the same `EARNINGS` payload's `annualEarnings[]` reveals the fiscal-year-end month, from which the fiscal-quarter number is derived by the target `fiscalDateEnding`'s position in that fiscal year (Apple Dec-2023 → `2024Q1`). No OVERVIEW call, so the budget accounting is unchanged; the alternate-label retry stays as the belt-and-suspenders.
- **Paid-tier bypass (was escalated) — RESOLVED by collapsing to one knob.** Instead of an `ALPHAVANTAGE_TIER` env + `isAlphaVantagePaidTier()` (speculative tier vocabulary with one internal consumer, flagged YAGNI in review), the guard uses a single `ALPHAVANTAGE_DAILY_LIMIT` where **`0` = unlimited**. A paid user sets `ALPHAVANTAGE_DAILY_LIMIT=0` — which honors the issue's "no-op on a paid tier" outcome *and* removes the marketing surface from the provider's public API (BP-038). No separate tier concept ships. *(Confirm at review if a distinct `ALPHAVANTAGE_TIER` label is still wanted for readability; default is the single knob.)*
- **`get_analyst_estimates` provenance — RESOLVED: primary-wins.** `source` stays `"finnhub"` whenever the Finnhub baseline answered (it's the bulk of the payload); `"alphavantage"` only when Finnhub is absent and AV filled something. Avoids tagging a mostly-Finnhub payload as AV (§4, §7 test #5). The real-provider goal check (§7) asserts per these rules, not a blanket `source: "alphavantage"`.
- **Transcript first request must use the derived fiscal label — RESOLVED: fiscal-first, not calendar-first-then-retry.** The prior wording pinned the fiscal-year-end *derivation source* (`annualEarnings[]`) but left the first `EARNINGS_CALL_TRANSCRIPT` request's label ambiguous, so a lazy impl could request the calendar quarter first and lean on the alternate-label retry for every non-calendar issuer — making Apple-like lookups cost 3 budget units on the happy path instead of 2. §4 now **requires the first request to carry the derived fiscal label**, keeping the retry the exception (empty/rejected first attempt) rather than the rule. §7 test #4's happy-path case asserts the FIRST transcript URL carries the fiscal label (`2024Q1`) and resolves in exactly 2 units — failing both a calendar-only mapper and a calendar-first-then-retry impl.
- **AV `Error Message` bodies are request-shape errors, not quota throttles — RESOLVED: distinct `AlphaVantageRequestError`, retry-eligible on the first transcript label.** The body-error detection originally lumped `Error Message` with `Note`/`Information` under `AlphaVantageRateLimitError`, but AV returns `Error Message` for an unrecognized param (e.g. a mis-spelled `quarter` label) while `Note`/`Information` signal a spent frequency/premium quota — conflating them would (a) make a bad-label response degrade immediately instead of taking the alternate-label retry, and (b) mislabel a shape error as a rate limit in the §6 error table. §4 now splits `Error Message` → `AlphaVantageRequestError` (a request-shape error the first-label transcript attempt retries on, alongside empty `transcript[]`) from `Note`/`Information` → `AlphaVantageRateLimitError` (quota spent, degrade, no retry). §7 test #1 asserts `{ Error Message }` → `AlphaVantageRequestError` and test #4's retry case fires on it.

**Genuinely open (a call is welcome; none blocking):**

1. **AV `quarter`-label exact spelling for the transcript endpoint** — the *derivation source* is now pinned (fiscal-year-end month from the `EARNINGS` `annualEarnings[]`, §4), so the only remaining open item is confirming AV's exact `YYYYQn` string spelling against the live API during the record step. The alternate-label retry keeps it robust, but verifying the primary label keeps the retry the exception, not the rule.
2. **AV `INSIDER_TRANSACTIONS` server-side date filter** (`from=`) — the AV docs list `from=YYYY-MM-DD` as an optional filter, so the plan **passes it and §7 test #3 asserts the fetch URL carries it** (no longer treated as unverified). The only implementation-time check left is the exact param name against the live API during the record step; if it turns out unsupported, drop it and the client-side window filter stands alone.
