# FIX-859: Upgrade FSD from AI SDK 6 to AI SDK 7 (core, engine, tools) — Implementation Spec

> **Linear issue:** [FIX-859](https://linear.app/fixpoint-labs/issue/FIX-859/upgrade-fsd-from-ai-sdk-6-to-ai-sdk-7-core-engine-tools)
> **Status:** In Spec Review · **Category:** Enabler / Improvement · **Size:** Large (multi-package, 1 PR)
> **Necessity verdict:** Build as scoped. Dependency upgrade / enabler — the wrapper-over-primitive gate does not apply; nothing else in the AI SDK 7 project can start until this lands.

> **Note on references:** exact line numbers were accurate at authoring time and rot as code moves. File + symbol references are the durable part.

## 1. TLDR

**Plain-language summary.** The framework talks to AI models through a third-party library that just shipped a new major version. This work moves the framework onto that new version without changing how anything behaves: streaming answers, tool use, structured output, caching, and model fallback must all work exactly as before. It has to land first because every other item in this project builds on the new version.

**Technical statement:** Bump `ai` from `^6.0.93` to `^7`, plus the paired `@ai-sdk/*` provider and MCP package majors, across the whole workspace; re-align the model adapter (`createAiSdkModelResolver`) and its satellites to v7's surface; and prove behavioral parity with the existing mocked suites plus real-LLM goal checks.

**Deliverables:**

- Core: (TO FILL — exact resolver changes after research integration)
- Tools: bump `@ai-sdk/mcp`, re-verify `tool()` usage in `create-bash-tool.ts`
- Engine: verify duck-typed error classification; resolve the vestigial `ai` dependency pin
- Tests: update `MockLanguageModelV3` usages if the LanguageModel spec version bumps
- Workspace: bump `ai` / `@ai-sdk/*` pins in apps, labs, examples to keep one SDK major per graph
- Docs: migration note + version references (per §9)
- Changeset (`minor`) for core, engine, tools

**Size:** Large — multi-package, but a single PR (the workspace must move atomically; a split would leave mixed SDK majors mid-stream).

## 2. Overview

[FIX-859](https://linear.app/fixpoint-labs/issue/FIX-859/upgrade-fsd-from-ai-sdk-6-to-ai-sdk-7-core-engine-tools) upgrades the framework's generator provider layer from Vercel AI SDK 6 to AI SDK 7. The framework's own public API does not change; this is a mechanical upgrade with a behavioral-parity gate. It blocks the rest of the "AI SDK 7 Upgrade & Realignment" project (reasoning simplification, granular timeouts, file parts, telemetry, agent abstractions — all deferred to sibling issues).

## 3. Background & Research

### 3.1 Where FSD touches the AI SDK (complete inventory)

The v6 contact surface is narrower than it first appears. One file is the real migration target; everything else is satellites.

**Direct imports of `ai`:**

| File | Imports | Role |
| --- | --- | --- |
| `packages/core/src/models/createAiSdkModelResolver.ts` (~1,073 LOC) | `generateText`, `streamText`, `Output`, `stepCountIs` | **The adapter.** Builds requests (tools, `prepareStep`, `stopWhen`, `providerOptions`, `abortSignal`, `maxOutputTokens`, `onError`), consumes `fullStream` parts and normalizes them to framework `GeneratorModelStreamChunk`s, maps finish metadata/usage, wraps `AI_NoObjectGeneratedError`/`AI_NoOutputGeneratedError`, resolves provider search tools (`webSearch_20250305`, `webSearch`, `googleSearch`). Publicly exported as `createAiSdkModelResolver` and `wrapAiSdkModel`. |
| `packages/core/src/blocks/generator.ts` | `jsonSchema` | `normalizeToolSchema` wraps raw JSON Schema objects; detects AI SDK schema wrappers via `Symbol.for("vercel.ai.schema")`. Otherwise consumes only framework-normalized chunks (`text_delta`, `reasoning_delta`, `tool_call_delta`, `tool_result`, `source_url`, `finish`) — insulated from the SDK taxonomy by the resolver boundary. |
| `packages/tools/src/bash/create-bash-tool.ts` | `tool` | Defines the bash/readFile/writeFile AI SDK tools. |
| `packages/tools/src/mcp/manager.ts` | dynamic `import("@ai-sdk/mcp")` | `createMCPClient` for MCP transports. |

**Duck-typed contact (no imports, but version-sensitive shapes):**

| File | Shape depended on |
| --- | --- |
| `packages/core/src/models/caching.ts` | `providerOptions.anthropic.cacheControl { type: "ephemeral", ttl }`, `providerOptions.gateway.caching: "auto"`, `model.provider` strings (`"anthropic.chat"`, `"gateway.chat"`, …) |
| `packages/core/src/models/fallbackModel.ts` | `error.isRetryable`, `LoadAPIKeyError` / `AI_LoadAPIKeyError` constructor names, `statusCode` |
| `packages/engine/src/errors/classify-provider-error.ts` | `Symbol.for("vercel.ai.error")` marker, `name` tags, `statusCode` — structural on purpose (module-realm safe) |
| `packages/core/src/models/createModelResolver.ts` | Dynamic `require` of `@ai-sdk/anthropic` (`createAnthropic`), `@ai-sdk/openai` (`createOpenAI`), `@ai-sdk/google` (`createGoogleGenerativeAI`), `@ai-sdk/gateway` (`createGateway`), `@openrouter/ai-sdk-provider` |
| `packages/core/src/models/makeSchemaStrict.ts` | OpenAI strict-mode JSON Schema constraints, feeds `Output.object` |

**Tests:** 7 files under `packages/core/test/models/` construct `MockLanguageModelV3` from `ai/test` (48 instantiations). The integration-tests package uses framework-level generator mocks and is insulated from SDK internals.

**Notable finding — engine's pin is vestigial.** `packages/engine` declares `ai@^6.0.93` in `dependencies` but has zero compile-time imports of it (`classify-provider-error.ts` is deliberately structural). See Key Decision 4.

**Workspace dependents outside the issue's named packages:** `apps/kitchen-sink` (`ai@^6.0.97`, `@ai-sdk/openai`, `@ai-sdk/gateway@^3.0.51`, `@ai-sdk/mcp@^1.0.36`), `examples/hello-chat`, `labs/trading-desk` (`ai`, `@ai-sdk/gateway`, `@ai-sdk/xai`), `labs/knowledge-base` (`@ai-sdk/gateway`). These pass provider `LanguageModel` instances into core; mixed SDK majors in one dependency graph risks LanguageModel spec-version interop failures. See Key Decision 2.

### 3.2 AI SDK 7 breaking changes relevant to this surface

(TO FILL — from industry research agent, with links)

### 3.3 Key Decisions & Ramifications (top 5)

1. **(TO FILL after research)** — codemod-first vs hand-migration of the resolver.
2. **Bump the entire workspace atomically, not just core/engine/tools.** Alternative rejected: bump only the three named packages. Apps/labs construct provider models (`openai(...)`, gateway models) and hand them to core; a v6 provider model implementing an older LanguageModel spec version handed to a v7 `streamText` is exactly the interop failure a major exists to prevent. Ramification: the PR touches every workspace `package.json` that pins `ai`/`@ai-sdk/*`, and downstream consumers of published FSD packages must bump their own `ai` when they use the escape hatches (`wrapAiSdkModel`, `providerTools`, raw provider models) — this is the content of the migration note.
3. **Behavioral parity is the gate; no new v7 capability adoption.** Alternative rejected: adopt v7 features opportunistically while in the file. Ramification: sibling issues stay unblocked and reviewable; some code may look v6-idiomatic on v7 until siblings land.
4. **Drop engine's vestigial `ai` dependency rather than bump it.** Alternative rejected: bump the unused pin. Engine's only AI SDK contact is structural (symbol/name/statusCode). Ramification: engine no longer expresses a version constraint on `ai`; the classifier's structural detection must be re-verified against v7 error shapes (it was built for realm-safety, which also buys version tolerance). BP-038: subtract before you add.
5. **(TO FILL — mock strategy / spec-version decision after research).**

## 4. Technical Design

(TO FILL — per-file change plan after research integration)

## 5. Implementation Sequence

(TO FILL)

## 6. Edge Cases & Error Handling

(TO FILL)

## 7. Testing Strategy

**Goal & goal check (required).** Goal: a user's flows behave identically on v7 — streaming text/reasoning/tool deltas render live, structured output validates, multi-step tool loops terminate, caching markers still hit, fallback still falls back. Goal check (real model, out of CI):

1. `pnpm tsx goals/structured-output/recovers-off-schema-output/run.mts` — exercises real structured output + coercion repair on GLM 5.2 (PASS/FAIL printed by the script).
2. `cd apps/kitchen-sink && pnpm fsdev run kitchen-sink chat-agent -i '{"message":"…","mode":"ask"}'` — real streaming + tool loop over the default model group; pass = completed run with assistant message items and no `execution_error` in the NDJSON.
3. A caching spot-check: run 2 twice with an Anthropic key and confirm `cachedInputTokens` / cache-read provider metadata is non-zero on the second run (parity for FIX-423).

**Discipline:** `fsd:tdd` is the routed discipline (Improvement label), but the work is migration-shaped: the "red" is the existing suites failing on the bumped dependency; "green" is restoring them without weakening assertions. New tracer tests are only added where v7 introduces a behavior the suite didn't pin (each such case gets a regression test in `packages/core/test/models/`).

**CI seams:**

- `packages/core/test/models/*.test.ts` — the resolver contract suite (mock spec-version update per §research).
- `packages/core/test/generator-repair.test.ts`, structured-output/coercion tests — strict-schema parity (FIX-279, FIX-841).
- `packages/integration-tests` — full composition on mocked generators (unchanged by design; a failure here means the resolver boundary leaked).
- `pnpm typecheck` across the workspace — catches removed/renamed SDK types in apps/labs too.

## 8. Non-Goals

- **No adoption of new v7 capabilities** — reasoning/runtime-context simplification, granular timeouts, `uploadFile`/file parts, SDK telemetry, `WorkflowAgent`, `HarnessAgent` are sibling issues in the AI SDK 7 project.
- **No adapter re-architecture.** If migration reveals the resolver should sit on more of v7's public surface, that is flagged as a follow-up, not folded in (the issue explicitly asks for the flag, not the rewrite).
- **No behavior changes to gateway fall-through (FIX-609), caching defaults (FIX-423), or strict-schema handling (FIX-279)** — parity only.
- (TO FILL — deepening opportunities)

## 9. Documentation Plan

(TO FILL — from docs scoping agent)

## 10. Dependencies

- **Blocked by:** nothing in-repo. No open PR or branch touches `packages/core/src/models/`, `generator.ts`, or the tools package (verified against all open remote branches as of 2026-07-02).
- **Blocks:** the rest of the "AI SDK 7 Upgrade & Realignment" project.
- **External:** `ai@7`, paired `@ai-sdk/*` provider majors, `@ai-sdk/mcp` compatible major, `@openrouter/ai-sdk-provider` compatibility (TO CONFIRM from research).

## 11. Open Questions

(TO FILL)
