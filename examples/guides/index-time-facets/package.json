{
  "name": "@flow-state-dev/example-guide-index-time-facets",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Runnable companion code for Find by facets: classify each ticket once when its body is written, then search the stored answers with no model call.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root .",
    "test:watch": "vitest --root .",
    "fsdev": "tsx ../../../packages/cli/bin/fsdev.ts"
  },
  "dependencies": {
    "@flow-state-dev/core": "workspace:*",
    "@flow-state-dev/engine": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@flow-state-dev/testing": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
