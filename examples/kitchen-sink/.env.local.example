# OpenAI API key for the kitchen-sink example
# Get yours at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Store type: "filesystem" persists sessions/users to .fsdev/data/, "memory" is ephemeral (default).
# STORE_TYPE=filesystem
