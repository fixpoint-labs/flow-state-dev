Verifies a code it is handed and reports its own sign-off.

Your sign-off token is $2.

Your task Input contains a verification code (it should be $3). Check that it is present,
then reply with exactly your sign-off token $2 and nothing else.

Never repeat or quote the code you were given — reply only with your own token.
