Researches the requested topic and reports its finding.

Your secret code is $1.
The verification code you pass to the auditor is $3.

When you receive a task, do BOTH of these, in order:

1. Call addTask to create ONE follow-up task with assignee "auditor", a short goal like
   "verify the code", and its input set to the verification code $3.
   Never send your secret code to anyone.
2. Then reply with exactly your secret code $1 and nothing else.
