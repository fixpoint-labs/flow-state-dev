# workforce-conventions › its code comes from files alone

**Issue:** FIX-1357
**Outcome:** Someone adds a worker kind to their app by writing one file in `workforce/flows/workers/`, names it from a `WORKER.md`, and the seat hires and runs. No kind is written anywhere in the app's source — not on a `hireWorkforce` call, not in a barrel, not in a registry line — and the app behaves the same deployed as it does locally, because what registers the kind is a committed module of static imports rather than a scan that happens while the app runs.
**Input:** `apps/kitchen-sink/workforce/` — one team (`support`), two seats (`ada`, `grace`) whose files both name `flow: desk-clerk` and declare different `desk:` values, one custom kind at `flows/workers/desk-clerk.ts`, one custom block at `blocks/desk-note.ts`, and the committed `workforce.gen.ts`. Held out: `desk` is declared only in the two `WORKER.md` files and read only by the kind's own schema — nothing in the runner supplies it.
**Signal:** Both seats hire under `support.ada` and `support.grace`; each one's `answer` action, run over the real HTTP route **against the app as built for Next**, comes back naming the desk *its own file* declared (`front` for ada, `back` for grace); and a scan of `apps/kitchen-sink/**` outside `workforce.gen.ts` finds no literal that registers a kind. PASS prints both seats, both desks and the scanned file count; any mismatch is a FAIL.
**Anti-game:** A hollow pass would assert that two seats came back, or that the action returned 200 — both survive an implementation that hired every seat onto one shared configuration, or onto the built-in `agent` kind, which would not know `desk` at all. So the check grades on a value only the discovered kind declares and only the seat's own file supplies: the two seats sit on **one** kind and differ only in `desk`, so anything keyed by kind rather than by seat gives both the same answer and fails. The source scan is the inverse probe — it fails if the kind reaches the registry by any route other than the generated module, which is the only thing that makes "from files alone" true rather than merely convenient. It scans **registration routes**: every source file under the app except the shell's names module (`lib/workforce-shell.ts`, which names each kind's answering action for the page), and the `test/` and `e2e/` trees, none of which is served as a flow. Naming a kind there is not registering it; a hand registration anywhere else still fails the leg. Running against the **Next-built** app rather than the source tree is what makes it a check of the decision: a design that scanned the tree at run time passes on plain Node and returns an empty map here.
**Model:** scripted; the goal grades the desk tag, not the model. Since FIX-1589 the clerk's `answer` calls a model, so `next start` runs with `KITCHEN_SINK_TEST_MODE=1` (kitchen-sink's scripted model, keyless). The tag is written by the kind from each seat's own settings; the words after it are the script's and are not graded.
**Run:** `pnpm tsx goals/workforce-conventions/code-comes-from-files-alone/run.mts`

## Verdict log
| Date | Commit | Model | Verdict | Notes |
|------|--------|-------|---------|-------|
| — | — | — | NOT RUN | `run.mts` is not written and this check has never been executed. Blocked on FIX-1367: `desk-clerk.ts` composes `workerConfigSchema()`, which that issue publishes, so kitchen-sink does not compile and neither the Next build nor the hire can run. Write the runner and take the first verdict once FIX-1367 has landed — do not record a PASS from the source tree in its place, since the Next-built half is the decision this check exists to prove. |
| 2026-09-17 | 8f22506e6 | none | NOT RUN | **The FIX-1367 blocker above is cleared and was not the only one.** `workerConfigSchema` is published, `pnpm typecheck` is 71/71, and `pnpm build` (`fsdev gen && next build`) succeeds — so the compile half is genuinely fixed. The check still cannot run, for a reason this contract never recorded: **the seats are not reachable over HTTP.** `apps/kitchen-sink/workforce/hire.ts` is imported by nothing — the subtree is isolated on purpose, as its own header states — and `app/api/flows/[...path]/route.ts` serves only what is in `createFlowState({ flows })`, a **required static map** with no lazy provider and no post-construction registration. Serving `support.ada` / `support.grace` therefore needs a top-level `await` in `fsdev.config.ts`, which both the Next route handlers and the `fsdev` CLI import: an architecture change to the reference app's boot path, not wiring. Still NOT RUN rather than a weaker pass — running the seats through a standalone `@flow-state-dev/node` server would drop exactly the Next-built half the Anti-game field calls the point. **Open decision:** either wire the seats into the app's boot (expanding this issue's scope) or narrow this goal's Signal to drop the HTTP/Next-build clause. Do not take a verdict until that is settled. |
| 2026-09-17 | 9b5d4f6cb | none | NOT RUN | **Asked whether the Next-built guarantee could be observed from the build output instead of over HTTP — it cannot, and the reason is worse than "not served".** Measured on a real `pnpm build`: **zero compiled chunks** under `.next` contain `desk-clerk` or `desk-note`. Positive control on the same grep, so the absence means something: every registered flow (`chat-agent`, `rich-text-component`, `weekly-digest`) appears in **5** compiled chunks each. The only build-output traces of the workforce are `.nft.json` file-trace manifests listing the **source `.ts` files** for deployment inclusion, and `.tsbuildinfo`. **The bundler never resolved `workforce.gen.ts`'s static imports at all**, because `hire.ts` is unreachable from every Next entry point. So an assertion on the built output would be **red today, not green**, and there is no mechanism in the bundle to relocate the check onto. What `next build` does give this module is its TypeScript pass — real, but a different guarantee (it typechecks) from the one this goal exists to prove (its imports survive bundling). |
| 2026-09-21 | 53a5e279d | none | **PASS** | **First verdict.** `run.mts` is written and the seats are reachable: `apps/kitchen-sink/fsdev.config.ts` awaits `hireKitchenSinkWorkforce()` and spreads the seats into the map `createFlowState` serves (FIX-1429). All three legs green on one run. **Build:** compiled chunks under `.next` naming each kind — `chat-agent` 5, `rich-text-component` 5, `weekly-digest` 5 (the positive control, asserted before the rest, so a zero below would mean something), `desk-clerk` **3**, `desk-note` **3**, up from 0 and 0. **HTTP, against `next start` on the Next-built app:** `support.ada` answered `[front desk] …` and `support.grace` `[back desk] …` — one kind, two seats, each carrying the desk read out of its own `WORKER.md` at check time rather than written into the check. **Inverse probe:** 160 source files scanned under `apps/kitchen-sink`; the only literal naming `desk-clerk` is in `workforce.gen.ts` and in the kind's own module. Falsified both ways before it was trusted: reverting the config change fails leg (a) with both counts at 0, and reverting only `desk-note`'s emit fails leg (b) with both seats answering nothing. |
| 2026-09-21 | a362d23d6 | none | **PASS** | **Re-taken after folding review on #1989; two of the four fixes changed what this check proves, so the previous row's evidence no longer describes the current runner.** Same three legs, same counts — `chat-agent` 5, `rich-text-component` 5, `weekly-digest` 5, `desk-clerk` **3**, `desk-note` **3**; `support.ada` `[front desk] …`, `support.grace` `[back desk] …`; 160 source files scanned. **What moved: (i)** the leg (b) utterance is no longer emitted by `desk-note`. The block is also the tool `support.otto` declares, and emitting from inside it published the model's tool argument as a client- and history-visible assistant turn on that path. It is now a `.tap` on the `answer` action's sequencer in the kind's own module, so leg (b) still observes it over HTTP and the tool path is silent — the previous row's "reverting only `desk-note`'s emit fails leg (b)" now reads on `desk-clerk.ts`. **(ii)** the inverse probe matched only the quoted token `"desk-clerk"`, which a hand wiring never has to write: importing the kind module binds it to an identifier, and the specifier `"./flows/workers/desk-clerk"` does not contain the quoted substring. Leg (c) now resolves each file's import specifiers against the kind module as well, and names which route a hit took. **Falsified, not assumed:** a module registering the kind as `{ [deskClerk.kind]: deskClerk }`, containing zero occurrences of the quoted name, passed the old probe and now fails leg (c) by name. Also tightened: readiness required only a non-throwing `fetch`, so a process answering 4xx/5xx counted as serving — a weaker form of the stale-server false pass the port refusal exists to close; readiness now requires 200, while the refusal still takes any answer. |
| 2026-09-26 | 081f6fe83 (main) | none | FAIL | **Taken on `main` before FIX-1589 changed anything, so the narrowing below is not a way around a red this branch caused.** Legs (a) and (b) green; leg (c) red, naming `e2e/talk-from-page.spec.ts`, `e2e/workforce-shell.spec.ts`, `lib/workforce-shell.ts`, `test/hired-seat-auth.test.ts`, `test/hired-seat-visitor-auth.test.ts`, `test/manager-seat.test.ts`, `test/picked-session-panel.test.tsx` and `test/workforce-admin.test.ts`. None registers the kind: the tests and specs name it to drive it, and `lib/workforce-shell.ts` names it to label the shell. |
| 2026-09-26 | FIX-1589 branch on 081f6fe83 | none | **PASS** | Leg (c) narrowed to registration routes (epic FIX-1592 cross-spec resolution C2): `test/`, `e2e/` and the shell's names module are excluded, every other file still scanned for both routes. The server now runs under `KITCHEN_SINK_TEST_MODE=1`, since the clerk answers with a model and the check is keyless. Chunks: `chat-agent` 5, `rich-text-component` 5, `weekly-digest` 5, `desk-clerk` 5, `desk-note` 3. `support.ada` said `[front desk] Test mode (no scenario sentinel matched).`, `support.grace` `[back desk] …`. 145 of 188 source files scanned. |
| 2026-09-26 | FIX-1589 branch on 081f6fe83 | none | FAIL (planted) | **The narrowing still catches a registration.** A planted `lib/planted-registration.ts` importing the kind module failed leg (c) by name (`imports its module`). Removed after the run. |
| — | — | — | **FINDING** | **Two functions named `validateSegment`, in two packages, enforcing overlapping rules about the same class of name — and structurally unable to see each other.** `packages/workforce/src/loader/segments.ts` validates tree segments and now refuses Windows device names; `packages/engine/src/stores/filesystem/resource-path.ts:65` exports its own `validateSegment`, validating resource-key segments, also refusing Windows device names, via its own `WINDOWS_RESERVED_NAMES` set. The duplication is **forced, not careless**: `workforce`'s boundary rule denies `engine`, so it cannot import the list even though the list is identical. The cost is already real — this PR's first cut of the workforce list used `com0`–`com9`, while engine's has always been the correct `com1`–`com9`; a shared list could not have drifted. Not a stale cross-package claim: engine's `{@link validateSegment}` refers to engine's own function. The honest fix is the canonical list moving to `contracts`, which both packages allow — a cross-package refactor, not this PR's. |
| — | — | — | **FINDING** | **The worked example is inert, and that is a defect in its own right, independent of this goal.** `apps/kitchen-sink/workforce/` ships two seats, a custom kind and a custom block to demonstrate the convention — and nothing imports, serves, or bundles any of it. The demonstration cannot be exercised by a reader, a test, or a deployment. Whatever is decided about this goal's Signal, that gap is real: the reference app documents a feature it does not run. Record it as its own issue rather than letting it ride on this contract. |
