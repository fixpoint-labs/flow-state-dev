# workforce-conventions › its code comes from files alone

**Issue:** FIX-1357
**Outcome:** Someone adds a worker kind to their app by writing one file in `workforce/flows/workers/`, names it from a `WORKER.md`, and the seat hires and runs. No kind is written anywhere in the app's source — not on a `hireWorkforce` call, not in a barrel, not in a registry line — and the app behaves the same deployed as it does locally, because what registers the kind is a committed module of static imports rather than a scan that happens while the app runs.
**Input:** `apps/kitchen-sink/workforce/` — one team (`support`), two seats (`ada`, `grace`) whose files both name `flow: desk-clerk` and declare different `desk:` values, one custom kind at `flows/workers/desk-clerk.ts`, one custom block at `blocks/desk-note.ts`, and the committed `workforce.gen.ts`. Held out: `desk` is declared only in the two `WORKER.md` files and read only by the kind's own schema — nothing in the runner supplies it.
**Signal:** Both seats hire under `support.ada` and `support.grace`; each one's `answer` action, run over the real HTTP route **against the app as built for Next**, comes back naming the desk *its own file* declared (`front` for ada, `back` for grace); and a scan of `apps/kitchen-sink/**` outside `workforce.gen.ts` finds no literal that registers a kind. PASS prints both seats, both desks and the scanned file count; any mismatch is a FAIL.
**Anti-game:** A hollow pass would assert that two seats came back, or that the action returned 200 — both survive an implementation that hired every seat onto one shared configuration, or onto the built-in `agent` kind, which would not know `desk` at all. So the check grades on a value only the discovered kind declares and only the seat's own file supplies: the two seats sit on **one** kind and differ only in `desk`, so anything keyed by kind rather than by seat gives both the same answer and fails. The source scan is the inverse probe — it fails if the kind reaches the registry by any route other than the generated module, which is the only thing that makes "from files alone" true rather than merely convenient. Running against the **Next-built** app rather than the source tree is what makes it a check of the decision: a design that scanned the tree at run time passes on plain Node and returns an empty map here.
**Model:** none — the kind's action is a handler that reads its own settings; no model is contacted.
**Run:** `pnpm tsx goals/workforce-conventions/code-comes-from-files-alone/run.mts`

## Verdict log
| Date | Commit | Model | Verdict | Notes |
|------|--------|-------|---------|-------|
| — | — | — | NOT RUN | `run.mts` is not written and this check has never been executed. Blocked on FIX-1367: `desk-clerk.ts` composes `workerConfigSchema()`, which that issue publishes, so kitchen-sink does not compile and neither the Next build nor the hire can run. Write the runner and take the first verdict once FIX-1367 has landed — do not record a PASS from the source tree in its place, since the Next-built half is the decision this check exists to prove. |
| 2026-09-17 | 8f22506e6 | none | NOT RUN | **The FIX-1367 blocker above is cleared and was not the only one.** `workerConfigSchema` is published, `pnpm typecheck` is 71/71, and `pnpm build` (`fsdev gen && next build`) succeeds — so the compile half is genuinely fixed. The check still cannot run, for a reason this contract never recorded: **the seats are not reachable over HTTP.** `apps/kitchen-sink/workforce/hire.ts` is imported by nothing — the subtree is isolated on purpose, as its own header states — and `app/api/flows/[...path]/route.ts` serves only what is in `createFlowState({ flows })`, a **required static map** with no lazy provider and no post-construction registration. Serving `support.ada` / `support.grace` therefore needs a top-level `await` in `fsdev.config.ts`, which both the Next route handlers and the `fsdev` CLI import: an architecture change to the reference app's boot path, not wiring. Still NOT RUN rather than a weaker pass — running the seats through a standalone `@flow-state-dev/node` server would drop exactly the Next-built half the Anti-game field calls the point. **Open decision:** either wire the seats into the app's boot (expanding this issue's scope) or narrow this goal's Signal to drop the HTTP/Next-build clause. Do not take a verdict until that is settled. |
| 2026-09-17 | 9b5d4f6cb | none | NOT RUN | **Asked whether the Next-built guarantee could be observed from the build output instead of over HTTP — it cannot, and the reason is worse than "not served".** Measured on a real `pnpm build`: **zero compiled chunks** under `.next` contain `desk-clerk` or `desk-note`. Positive control on the same grep, so the absence means something: every registered flow (`chat-agent`, `rich-text-component`, `weekly-digest`) appears in **5** compiled chunks each. The only build-output traces of the workforce are `.nft.json` file-trace manifests listing the **source `.ts` files** for deployment inclusion, and `.tsbuildinfo`. **The bundler never resolved `workforce.gen.ts`'s static imports at all**, because `hire.ts` is unreachable from every Next entry point. So an assertion on the built output would be **red today, not green**, and there is no mechanism in the bundle to relocate the check onto. What `next build` does give this module is its TypeScript pass — real, but a different guarantee (it typechecks) from the one this goal exists to prove (its imports survive bundling). |
| — | — | — | **FINDING** | **The worked example is inert, and that is a defect in its own right, independent of this goal.** `apps/kitchen-sink/workforce/` ships two seats, a custom kind and a custom block to demonstrate the convention — and nothing imports, serves, or bundles any of it. The demonstration cannot be exercised by a reader, a test, or a deployment. Whatever is decided about this goal's Signal, that gap is real: the reference app documents a feature it does not run. Record it as its own issue rather than letting it ride on this contract. |
