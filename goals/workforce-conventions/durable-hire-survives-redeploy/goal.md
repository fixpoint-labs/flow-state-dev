# workforce-conventions › a hired team survives a redeploy

**Issue:** FIX-1475
**Outcome:** Someone hires a team into a running app over its ordinary HTTP route, redeploys, and the team is still there — the seats answer at the same addresses, carrying the settings the hire supplied rather than any default. Hiring writes the seat down; the next start reads it back. A stored seat the current code can no longer run costs one seat, not the deployment.
**Input:** `apps/kitchen-sink`, built for Next and served by `next start`, with `STORE_TYPE=filesystem` so one store outlives the process, and `WORKFORCE_ADMIN_TOKENS` configuring an admin credential for two organizations (`acme`, `bravo`). Held out: the settings of the seat leg 3 grades are a **token generated at check time** — nothing in the app, the tree or this file can supply it.
**Signal:** Six legs on one run. (0) the file-declared `support.ada` answers naming its own desk, asserted first and re-asserted after every restart. (1) a hire over `workforce-admin` answers straight away in the hiring process; a hire naming a kind the app does not carry is refused and leaves no address. (2–3) a seat hired with a check-time token answers **after a restart** and its answer carries **that token**. (4) a stored row edited to name a kind the code does not carry leaves the app serving, the other seats answering, that one address 404ing, the boot naming the skip, and the bad row unrepaired. (5) firing the token seat and restarting leaves a 404 that no start brings back. PASS prints the token seat's answer, the degrade evidence and the fire evidence; any mismatch is a FAIL.
**Anti-game:** A hollow pass would assert that the seat answered 200 after a restart — which an app that re-read the same `WORKER.md` files, or hired every seat onto one shared default, passes without storing anything. So leg 3 grades a value **only the stored row can carry**: a token minted by this run, sent in the hire's settings, and read back out of the answer. A seat coming back on a file-declared default or a kind's own default cannot produce it. The store directory is **removed before the run**, so a roster an earlier run left behind cannot be what passes. The file-declared control in leg 0 is the inverse probe: it is asserted before everything and re-asserted after every restart, so a zero on a durability leg is distinguishable from an app that simply is not serving. Leg 4 edits a row **the app itself wrote** rather than fabricating one, because a fabricated row tests the parser while the real failure is a kind deleted from the source after the row was written. Running against the **Next-built** app rather than the source tree is what makes it a check of the decision: a roster read lazily on first request, or held in module memory, behaves differently across a real process boundary.
**Model:** none — the seat's action is a handler that reads its own settings; no model is contacted.
**Run:** `pnpm tsx goals/workforce-conventions/durable-hire-survives-redeploy/run.mts`

## Verdict log
| Date | Commit | Model | Verdict | Notes |
|------|--------|-------|---------|-------|
| 2026-09-21 | 3e24c1f9d | none | **PASS** | **First verdict, on the real path: `next start` against the Next-built app, all six legs green on one run.** Build: `pnpm packages:build` then `pnpm --filter @flow-state-dev/kitchen-sink build`, both clean. **Control:** the file-declared `support.ada` answered `[front desk] …` before anything else and again after every restart, so every zero below means something. **Durability:** `acme.support.di` was hired with `desk` set to a token minted by this run, the process was stopped and restarted from the same build, and it answered `[desk-bf298a88-0760-4051-9995-b3a7af8f7afc desk] is the printer fixed?` — carrying that token, which no `WORKER.md` and no kind default can produce. **Admission:** a hire naming a kind the app does not carry was refused and left no address. **Org fence:** a hire whose *body* named `bravo` under `acme`'s credential wrote its row under **acme**, and no `bravo` org record was ever created. **Degrade:** a stored row edited to name a missing kind left the app serving, `acme.support.bo` 404ing, the boot naming the skip, and the row **unrepaired** on disk. **Fire:** firing `acme.support.di` and restarting left a 404 with the row gone from storage. **Falsifiability — all three of `PLAN.md`'s red states produced on this branch, each observed and reverted, with the check re-run green after every revert.** (1) *The boot reload registers nothing* — the reload loop in `fsdev.config.ts` sliced to empty: leg 3 red with `acme.support.di did not survive the restart — it 404s, so the hire was never written down`, and the control silent, so the zero is durability and not a dead app. `PLAN.md` spells this sabotage `for (const seat of [])`, which **does not compile** — TypeScript narrows `[]` to `never[]` and the build dies on `seat.id` before the check ever runs, so it was re-aimed to `reload.seats.slice(0, 0)`, which registers nothing and still typechecks. (2) *The hire is never written down* — the `rosterOf(ctx).create(…)` row write removed from the admin flow's hire, registering in process only: leg 1 still green (the seat answers straight away in the hiring process) and leg 3 red with the same 404 text. That pair **is** the *hired* / *written down* split the issue exists for. (3) *The seat comes back on a default* — `hiredSeatManifest`'s `...row.settings` dropped so `desk` falls to its schema default: leg 3 red with `answered after the restart but said "[front desk] …", which does not carry the token this check generated … so it came back on a default rather than on the settings the hire supplied`. Leg 1 went red too, which `PLAN.md` did not predict: the same mapping mints the in-process seat, so the check catches this defect one leg earlier than the plan claimed. **A property of the check, found only by producing these:** `runGoal` reports a *thrown* error as the sole failure and discards the legs already graded, so red state (2) printed leg 4's setup throw (`expected exactly one stored roster row for "support.bo" … found 0`) instead of leg 3's reason; leg 3's own text was recovered by stopping the runner after leg 3. The check goes red either way — only its wording misleads. Recorded, not fixed. |
| 2026-09-21 | — | — | *superseded* | **An earlier `NOT RUN` on this contract was wrong, and the way it was wrong is worth keeping.** It read: *`pnpm build` fails at `packages/patterns/src/debate/index.ts:314`, verified pre-existing on pristine `origin/main`.* Both halves were true and the conclusion did not follow. Reproducing a failure on `main` establishes only that this branch did not cause it — **not** that the build is broken. `apps/kitchen-sink`'s `build` script is `fsdev gen && next build` and does not build packages, so in a worktree with no `dist/` it typechecks `packages/**` **source** instead of emitted `.d.ts`; that is why the error landed on a source line in a package this app only consumes. CI (`.github/workflows/ci.yml`) runs `pnpm packages:build` first, which is why it stayed green throughout. The check was aimed at a *neighbour* of the claim — BP-003's first shape — and the fix was one command. |
| — | — | — | **FINDING** | **`GET /api/flows` is unauthenticated and now enumerates every organization's hired seats by address.** Executed on this branch, not read: with two seats registered at `acme.support.ada` and `bravo.support.ada`, a `GET /api/flows` carrying **no credential at all** returned `200` and listed both by full id, alongside each one's `kind`, `cardinality`, `requireUser`, `requiresOrg`, action names and full action input schemas. The route is classified `{ kind: "exempt" }` in `routes/route-auth.ts` and answered from `registry.list()` in `routes/http-handlers.ts`. Not leaked: a seat's `settings`, its `instructions`, sessions, or team membership — the `config` bag is not projected. Nothing in the listing marks a row as org-owned either: `requiresOrg` is derived from what a flow's *blocks* declare, and read `false` for an org-owned instance in this run, so it cannot be used to post-filter. The endpoint was harmless while it listed demo flow *shapes*; a durable hire is what turns it into an inventory of customers. **Owned by FIX-1486, consumed and not owned here (ER-17)** — recorded rather than fixed, and disclosed to readers in `apps/docs/docs/workforce/durable-hire.md`. |
