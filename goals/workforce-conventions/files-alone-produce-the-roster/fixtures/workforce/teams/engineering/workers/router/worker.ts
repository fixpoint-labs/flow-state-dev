// A seat whose shape is code rather than a document. The loader records the
// path to this file and never imports it — the marker below is how the goal
// proves the path it recorded points here and not somewhere else.
//
// marker: KESTREL-7781
export default {};
