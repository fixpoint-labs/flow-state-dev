# workforce-seats › a non-agent seat receives its skills

**Issue:** FIX-1367 (thin `WorkerConfig` admission for hireable kinds; builds on FIX-1356's skills file convention and FIX-1325's seat factory)
**Outcome:** An app defines a worker flow kind with **no model and no generator in it**, composes the admission contract into its settings schema, and hires two seats from a workforce tree that declares skills at three levels — an `org/skills/` folder, a team's, and one beside a worker. Each seat runs an action to completion through the real HTTP route, and a block **nested inside that action** reads the skills its own folders declared off `ctx.flow.config.seatSkills`, in level order. Neither seat holds the other's team-level or own-folder skills; both hold the org folder's. With the contract stripped from that same kind, the whole roster refuses at the hire, the message names the worker and the fix, and nothing is registered.

**Why a non-agent kind.** The claim this issue makes is about *every hireable kind*, not about the built-in `agent`. An end-to-end run of the agent kind would be green for a mechanism that only ever worked on the one kind that happened to declare the key — which is exactly the defect being removed. The kind here calls no model, so a pass cannot be coming from anything an agent does.

**Input:** `fixtures/input.json` — the two seat ids, each one's own setting, and the skill names each one's folders declare. `fixtures/workforce/**` carries the same thing as files: `org/skills/house-style/`, `teams/support/skills/triage-tone/`, `teams/support/workers/desk/skills/escalation/`, `teams/billing/skills/dunning/`, and a `WORKER.md` per seat. `fixtures/no-contract/` is the control tree. Held-out: every id, setting and skill name is read from the fixture and graded against what the running blocks reported, so swapping any of them still passes a correct implementation. **Each seat's own setting differs from its schema's default on purpose** — a setting dropped on the way to the flow would otherwise read back as the default and pass.
**Signal:** against the real HTTP router (`createFlowApiRouter`) over on-disk SQLite stores, matching its siblings in this family. The records come from the real loader (`readWorkforce`), never hand-built: the folders are half of what is under test.
(a) `readWorkforce` reads the tree with no errors and produces two records; one `hireWorkforce` call turns them into two seats of the non-agent kind.
(b) Each seat runs `POST /api/flows/<id>/<session>/actions/run` to completion. After closing the stores, reopening the same file and building a fresh host, `GET /api/flows/sessions/<session>/state` shows what a block **nested inside the action** read from `ctx.flow.config`: its own skill names as a **sequence** in level order (org, team, own), and its own desk.
(c) Neither seat's list equals the other's; neither holds a skill from the other's team or worker folder; both hold the org folder's.
(d) A kind whose schema omits only `seatSkills`: the hire throws, naming the worker and `seatSkills`, returns nothing, and the good seats' addresses 404 on a host built from what came back. It omits **one** contract key rather than all of them on purpose — a control that drops the helper drops every key with it, and its refusal would then be equally consistent with hire checking whether the helper was called. It also runs its **own action set**, requiring only its `desk`: the delivery kind's action set contains a block that independently requires `seatSkills` through its `flowConfigSchema`, so a control sharing it would be refused by that block whether or not its schema could accept the imposed bag.
(e) A hand-written schema that never calls `workerConfigSchema()` but accepts everything hiring imposes is hired, carrying the imposed `seatSkills`. It runs the same `desk`-only action set as (d), so the pair differs in exactly one thing: whether its schema declares `seatSkills`. This is what makes (d) mean what it says: admission is decided by what a schema accepts, not by which helper built it.

**Anti-game:** a hollow pass would assert the hires returned and the requests completed — true even when every seat holds an empty catalog, or one shared one. So the check MUST read the skills back through `/state` rather than off the returned instance, because the claim is that the bag reached a RUNNING BLOCK and a bag asserted on the instance proves only the mint; MUST read from a **nested** block rather than the action root, since a root-only read proves nothing about the context spread that carries the value down; MUST close the stores and rebuild the host first, so nothing is served from a live context; MUST compare the names as an ordered **sequence**, since a set check passes for a bag assembled backwards and level order is part of the promise; MUST assert each seat does NOT hold its sibling's folders, since one shared bag is always right for somebody; MUST use a kind with no model, since an agent run cannot distinguish the contract from the agent kind's own path; MUST assert the control refusal left **nothing** registered, since a refusal after a partial hire is not a refusal; and MUST keep the control pair off any action set whose blocks require `seatSkills`, since such a block refuses the control on its own and lets the pair pass while hiring imposes nothing. That last property is checked statically by `goals/scripts/validate-control-shape.mts` (rule C4).
**Model:** n/a — handlers only, by construction. The property under test is which settings reach a running block.
**Run:** `pnpm tsx goals/workforce-seats/a-non-agent-seat-receives-its-skills/run.mts`

## Verdict log
| Date | Commit | Model | Verdict | Notes |
|------|--------|-------|---------|-------|
| 2026-09-17 | fix/FIX-1367 (pre-PR) | n/a (handlers only) | PASS | The loader read the three-level tree and both records hired into a kind with no model in it. Both ran through the real HTTP route; after closing the SQLite file and rebuilding the host, `support.desk`'s nested block showed `["house-style","triage-tone","escalation"]` and `billing.desk`'s showed `["house-style","dunning"]`, each with its own desk. Neither held the other's team or own-folder skills; both held the org folder's. With the contract stripped, the hire refused naming `support.legacy` and `workerConfigSchema()`, and the good seats' addresses 404'd. |
| 2026-09-17 | fix/FIX-1367 (control run, hire delivers no skills) | n/a | FAIL (expected) | With the factory imposing a hard-coded empty list instead of the record's skills, leg (b) went red for both seats — *"its nested block saw [], its folders declared […]"* — and legs (c) took it too. This is the control that proves the check reaches the delivery it claims to grade, rather than passing on the seats merely running. |
| 2026-09-17 | fix/FIX-1367 (control run, a seat handed a sibling's folder) | n/a | FAIL (expected) | Copying `teams/support/skills/triage-tone/` into `teams/billing/skills/` was caught by leg (c): *"billing.desk: holds its sibling's skills ["triage-tone"]"*, and by (b)'s sequence check. Confirms the isolation half is graded and not assumed. |
| 2026-09-17 | fix/FIX-1367 (control run, an action rejected before it runs) | n/a | FAIL (expected, and reports only what broke) | Addressing a seat that does not exist made both actions 404. The run reports `expected 202 … got 404` once per seat and **no** `request ended undefined` — the poll is gated behind the 202, so a rejected action is never waited on. It also fails in ~3.7s rather than spending 5s per seat polling an empty request id. The downstream legs still report (a `/state` 404, an empty skill list): those are consequences the check genuinely measured, not invented ones. Recorded because a check that emits a second, wrong failure beside the real one degrades diagnosis in the same way a check that measures the wrong thing does. |
| 2026-09-17 | fix/FIX-1367 (review round 7) | n/a (handlers only) | PASS | **The control was rebuilt, because it passed for the wrong reason** — the same defect found and fixed in the sibling goal, unswept here. It removed `workerConfigSchema()` and with it every contract key, so its refusal was equally consistent with hire checking whether the helper was *called*. It now declares `instructions` and `teamInstructions` and omits only `seatSkills`, leaving one cause, and leg (d) grades on that key rather than the helper's name. New **leg (e)**: a hand-written schema that never calls the helper but accepts the whole imposed bag hires exactly like a composed kind. Control run: giving the refusing kind its missing `seatSkills` makes it hire (`the kind with no contract hired 3 seat(s) instead of refusing`), which is what proves the refusal is caused by that key's absence. |
| 2026-09-17 | fix/FIX-1367 (review round 8) | n/a (handlers only) | PASS | **The control pair was green for the wrong reason, and the round-7 guard did not see it.** Both control flows reused the delivery kind's action set, whose `recordSkills` block independently requires `seatSkills` through its `flowConfigSchema`. Demonstrated rather than argued: simulating the regression *hire supplies `seatSkills` only for non-empty sets* left this goal **PASS**, while the sibling goal — whose control runs a `desk`-only block — went **FAIL** on the same regression. The pair now runs its own `controlActions` requiring only `desk`; under the same simulated regression the goal goes FAIL (*"the kind with no contract hired 3 seat(s) instead of refusing"*), and PASS with the regression removed. |
