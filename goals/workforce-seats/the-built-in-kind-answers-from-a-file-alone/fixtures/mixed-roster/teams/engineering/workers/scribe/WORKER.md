---
description: Names a flow kind nobody registered.
flow: reviewer
---

This worker can never hire: no kind named `reviewer` is passed, and the built-in
is `agent`. It shares its roster with a valid worker on purpose — that is what
makes the refusal a test of all-or-nothing admission rather than of the throw.
