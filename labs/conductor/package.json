{
  "name": "@flow-state-dev/conductor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "description": "Incubation lab: Conductor (LAB-138). A task row becomes a watched, settled coding run — a board, one handed-off harness manager per epic, a per-run checkout, and a verdict read before the row is settled.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root . --passWithNoTests",
    "test:watch": "vitest --root . --passWithNoTests",
    "fsdev": "tsx ../../packages/cli/bin/fsdev.ts"
  },
  "dependencies": {
    "@flow-state-dev/claude-code": "workspace:*",
    "@flow-state-dev/core": "workspace:*",
    "@flow-state-dev/engine": "workspace:*",
    "@flow-state-dev/harness-manager": "workspace:*",
    "@flow-state-dev/orchestration": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@flow-state-dev/testing": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
