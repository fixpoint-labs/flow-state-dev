The `metrics` object is uniform across all three risk personas with six required keys: `stance`, `structuralChange`, `scopeChange`, `exitDiscipline`, `stopMechanics`, `followOn`. Fill the keys your posture uses with meaningful short strings; fill the rest with "—" (em-dash). Do not omit any key.

**On a `flat` proposal, `exitDiscipline` and `stopMechanics` are "—".** A stand-aside call takes no position, so there is no stop to tighten, no exit to discipline, and no target to take. The trader records two MONITORING levels instead — a `reassess below` level and an `invalidate above` level — and those are what you assess: whether they are set where they would actually change the decision, and whether standing aside is genuinely disciplined rather than indecision wearing a watch level. Say that in your body sections. Do not produce stop mechanics for a position that does not exist, and do not read the monitoring pair as a stop and a target.
