{
  "name": "flow-state-dev",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "install:all": "pnpm install",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck && node scripts/validate-package-boundaries.mjs",
    "lint": "turbo run lint",
    "knip": "knip",
    "knip:ci": "knip --include unlisted,binaries,unresolved",
    "test": "turbo run test",
    "test:watch": "pnpm -r --parallel --stream --if-present test:watch",
    "build:packages": "turbo run build --filter=\"./packages/*\"",
    "devtool:dev": "pnpm --filter @flow-state-dev/devtool-app dev",
    "docs:dev": "pnpm --filter @flow-state-dev/docs dev",
    "docs:build": "pnpm --filter @flow-state-dev/docs build",
    "docs:serve": "pnpm --filter @flow-state-dev/docs serve",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build:packages && changeset publish --provenance",
    "release:ci": "pnpm build:packages && bash -eo pipefail -c 'if [ -n \"${NPM_TOKEN:-}\" ]; then changeset publish --provenance; else echo \"Skipping npm publish in CI: NPM_TOKEN is not set\"; fi'",
    "release:snapshot": "changeset version --snapshot && pnpm build:packages && changeset publish --tag canary --no-git-tag --provenance",
    "fsdev": "tsx packages/cli/bin/fsdev.ts"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@vitest/coverage-v8": "^3.2.4",
    "knip": "^6.14.2",
    "ts-morph": "^28.0.0",
    "tsx": "^4.19.4",
    "turbo": "^2.9.16",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
