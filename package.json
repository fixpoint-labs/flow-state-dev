{
  "name": "flow-state-dev",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "install:all": "pnpm install",
    "build": "turbo run build && pnpm build:assets",
    "build:assets": "pnpm --filter @flow-state-dev/devtool build:assets",
    "typecheck": "turbo run typecheck && node scripts/validate-package-boundaries.mjs && node scripts/validate-updater-purity.mjs",
    "lint": "turbo run lint",
    "knip": "knip",
    "knip:ci": "knip --include unlisted,binaries,unresolved",
    "test": "turbo run test --concurrency=1",
    "test:watch": "pnpm -r --parallel --stream --if-present test:watch",
    "packages:build": "turbo run build --filter=\"./packages/*\"",
    "devtool:dev": "pnpm --filter @flow-state-dev/devtool-app dev",
    "docs:dev": "pnpm --filter @flow-state-dev/docs dev",
    "docs:build": "pnpm --filter @flow-state-dev/docs build",
    "docs:serve": "pnpm --filter @flow-state-dev/docs serve",
    "storybook": "pnpm --filter @flow-state-dev/ui storybook",
    "kitchen-sink:dev": "pnpm --filter @flow-state-dev/kitchen-sink dev",
    "trading-desk:dev": "pnpm --filter @flow-state-dev/trading-desk dev",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm packages:build && changeset publish --provenance",
    "release:ci": "pnpm packages:build && bash -eo pipefail -c 'if [ -n \"${NPM_TOKEN:-}\" ]; then changeset publish --provenance; else echo \"Skipping npm publish in CI: NPM_TOKEN is not set\"; fi'",
    "release:snapshot": "changeset version --snapshot && pnpm packages:build && changeset publish --tag canary --no-git-tag --provenance",
    "fsdev": "tsx packages/cli/bin/fsdev.ts",
    "goal:all": "pnpm --filter @flow-state-dev/goals goal:all"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.5",
    "@vitest/coverage-v8": "^3.2.4",
    "knip": "^6.14.2",
    "ts-morph": "^28.0.0",
    "tsx": "^4.19.4",
    "turbo": "^2.9.16",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  }
}
