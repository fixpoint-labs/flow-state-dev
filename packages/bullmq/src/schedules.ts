/**
 * BullMQ-backed schedule runner. Maps ScheduleIndex entries to BullMQ
 * repeatable jobs (native cron syntax) and ticks the schedule on each
 * repeat.
 *
 * Stub — full implementation ships in a subsequent task.
 */
