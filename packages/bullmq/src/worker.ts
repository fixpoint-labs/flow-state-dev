/**
 * Flow-run job processor. The worker dequeues BullMQ jobs and calls
 * runAction for each one, mapping execution results back to BullMQ
 * job completion/failure semantics.
 *
 * Stub — full implementation ships in a subsequent task.
 */
