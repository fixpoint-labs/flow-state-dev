/**
 * `@flow-state-dev/claude-code` package root.
 *
 * Exposes the source-agnostic handle envelope. Import the in-process
 * Agent SDK surface from `@flow-state-dev/claude-code/sdk`.
 */
export * from "./shared";
