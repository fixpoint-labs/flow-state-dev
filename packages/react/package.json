{
  "name": "@flow-state-dev/react",
  "version": "0.0.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@flow-state-dev/client": "workspace:*",
    "@flow-state-dev/core": "workspace:*"
  },
  "peerDependencies": {
    "react": "^18.0.0 || ^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "react": "^19.2.4"
  },
  "scripts": {
    "build": "pnpm --filter @flow-state-dev/core build && pnpm --filter @flow-state-dev/client build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @flow-state-dev/core build && pnpm --filter @flow-state-dev/client build && node ../../scripts/typecheck.mjs",
    "lint": "echo 'lint not configured yet'",
    "test": "pnpm --filter @flow-state-dev/core build && pnpm --filter @flow-state-dev/client build && vitest run --root .",
    "test:watch": "pnpm --filter @flow-state-dev/core build && pnpm --filter @flow-state-dev/client build && vitest --root ."
  }
}
