{
  "name": "@flow-state-dev/ui",
  "version": "0.0.0",
  "private": true,
  "description": "shadcn-compatible component registry for Flow State UI",
  "type": "module",
  "files": [
    "registry",
    "api",
    "README.md"
  ],
  "scripts": {
    "build": "echo 'registry package: no build step required'"
  }
}
