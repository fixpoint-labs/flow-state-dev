---
description: The same person, hired into a kind that never declared `principal:`.
flow: desk
principal: u_dana
answersFor: approvals-desk
---

The control. `desk` is an ordinary agent-shaped kind whose closed config schema
does not declare `principal:`, so this roster must refuse by name.
