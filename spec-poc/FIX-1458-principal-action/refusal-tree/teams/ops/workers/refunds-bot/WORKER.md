---
description: The same bind, on a kind whose closed schema never declared `reviewedBy:`.
flow: plain
answersFor: approvals-desk
reviewedBy: u_dana
---

The control. `plain` is an ordinary worker kind that declares no review bind,
so this roster must refuse by the key's name rather than hiring a seat whose
accountability setting went nowhere.
