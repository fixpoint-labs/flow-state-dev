# FIX-1363 — Built-in agent flow kind · explainer

## 1. Today

```mermaid
flowchart LR
  W["WORKER.md<br/>instructions only"] -->|"no flow:"| H[hireWorkforce]
  H -->|refused| X["no seat"]
  A["app writes its own<br/>agent flow from scratch"] --> K["kinds: { myAgent }"]
  K --> H
  H --> S["working seat"]
```

The contract for the default worker is written and approved. Nothing implements it. A file that
carries only instructions is still refused, so the only way to get a working worker is to write
the whole flow yourself and register it.

## 2. After (proposed)

```mermaid
flowchart LR
  W["WORKER.md<br/>instructions only"] -->|"no flow:"| H[hireWorkforce]
  B["built-in agent kind<br/>merged underneath"] --> H
  K["kinds: { agent: yours }"] --> H
  H -->|"absent flow:"| Seat["working seat<br/>talks · shared skills"]
  H -->|"caller supplied agent"| Yours["the caller's flow,<br/>every seat"]
  H -->|"unregistered name"| X["refused, by name"]
```

Absent means the built-in; a name nobody registered still fails loudly. The seat talks — its
body is its instructions — and can use skills the app handed over. It has no memory and no tools
unless the app supplies a tool catalog.

## 3. Where the decision fell

```mermaid
flowchart TD
  I["FIX-1363<br/>build the kind"] --> D1["a factory, called with<br/>nothing = the built-in"]
  I --> D2["tools need an app-supplied<br/>catalog, or refuse loudly"]
  I --> D3["no memory key at all"]
  I --> D4["skills wired, shared<br/>across seats"]
  D1 -.->|rejected| R1["a finished flow constant"]
  D2 -.->|rejected| R2["drop tools, or pick<br/>tools for every app"]
  D3 -.->|rejected| R3["declare memory,<br/>switch it off"]
  D4 -.->|rejected| R4["ship no skills;<br/>next issue picks the stack"]
  I --> G["named gap:<br/>every seat shares one catalog<br/>until FIX-1362"]
```

Four calls, each with its rejected alternative on a dashed edge. The gap node is the honesty
debt this issue takes on: the word "skills" is true at this step, but it is the app's skills,
not each worker's own. FIX-1362 closes it, and the docs say so in the meantime.

_Panel 4 lands once the implementation merges._
