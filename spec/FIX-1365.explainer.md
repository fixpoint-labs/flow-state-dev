# FIX-1365 — Thin Proof: hire the OOTB `agent` kind

## 1. Today

The epic built the whole path. Every check stops one step before the answer.

```mermaid
flowchart LR
  subgraph proved["Proved today"]
    F["WORKER.md<br/>no flow: key"] --> H["hireWorkforce"]
    H --> S["seat minted<br/>instructions setting arrives"]
  end
  subgraph gap["Not proved anywhere"]
    S -.-> M(["model sees the body?"])
    M -.-> A(["a real answer"])
  end

  U["unit specs<br/>mock the model"] --> S
  G["goal checks<br/>always pass kinds:"] --> S

  style gap stroke-dasharray: 5 5
  style M fill:#fde68a,stroke:#b45309,color:#000
  style A fill:#fde68a,stroke:#b45309,color:#000
```

Re-derived by script, not asserted — `spec-poc/FIX-1365-evidence/check.mjs`:

| Claim | Result |
|---|---|
| `defineAgentWorkerFlow` in any goal file | **0** of 177 files |
| `hireWorkforce` call sites in `goals/` passing `kinds:` | **4** of 4 |
| Workforce goals that run a model | **0** of 3 |

## 2. After (proposed)

One goal check closes the dashed arrow — and grades the **answer**, not the settings bag.

```mermaid
flowchart TD
  T["fixtures/workforce/<br/>2 × WORKER.md<br/>description + body only"] --> R["readWorkforce"]
  R --> HW["hireWorkforce(workers)<br/><b>no kinds argument</b>"]
  HW --> K["built-in agent kind"]
  K --> SA["seat: engineering.desk"]
  K --> SB["seat: marketing.desk"]
  SA --> QA["same question<br/>over the real HTTP route"]
  SB --> QA
  QA --> GA["reply A must carry<br/>TIDEPOOL-4417 only"]
  QA --> GB["reply B must carry<br/>SANDPIPER-9023 only"]

  X["3rd roster<br/>flow: reviewer"] --> HW2["hireWorkforce"]
  HW2 --> XR["refused by name,<br/>lists 'agent',<br/>nothing registers"]

  style GA fill:#bbf7d0,stroke:#15803d,color:#000
  style GB fill:#bbf7d0,stroke:#15803d,color:#000
  style XR fill:#bbf7d0,stroke:#15803d,color:#000
  style HW stroke-width:3px
```

Two negative controls must be run and logged red: **bodies swapped** (each reply carries the
other's token) and **bodies emptied** (neither token appears).

## 3. Where the decision fell

```mermaid
flowchart TD
  Q1{"Where does<br/>the proof live?"}
  Q1 -->|"a demo app /<br/>kitchen-sink feature"| N1["rejected — ships product<br/>surface the epic ruled out"]
  Q1 ==>|"chosen"| Y1["one goal check<br/>under goals/<br/>(KS = cwd only)"]

  Q2{"What counts<br/>as proved?"}
  Q2 -->|"instructions reached<br/>ctx.flow.config"| N2["rejected — a sibling goal<br/>already proves it, and it<br/>holds even if the model<br/>never sees the body"]
  Q2 ==>|"chosen"| Y2["the model's reply text,<br/>graded as a pair"]

  Q3{"Does it prove<br/>memory?"}
  Q3 ==>|"chosen"| Y3["no — and goal.md<br/>says so out loud"]
  Q3 -->|"stretch to cover it"| N3["rejected — C5: the default<br/>mint has no memory;<br/>that proof is FIX-1364's"]

  style Y1 fill:#bbf7d0,stroke:#15803d,color:#000
  style Y2 fill:#bbf7d0,stroke:#15803d,color:#000
  style Y3 fill:#bbf7d0,stroke:#15803d,color:#000
  style N1 fill:#fecaca,stroke:#b91c1c,color:#000
  style N2 fill:#fecaca,stroke:#b91c1c,color:#000
  style N3 fill:#fecaca,stroke:#b91c1c,color:#000
```

**Scope, per the locked contract** (`docs/architecture/workforce-default-worker-kind.md`):
this issue owns acceptance criteria **1–3** only. Criterion 4 is FIX-1363's, 5 and 6 are
FIX-1362's, and memory is FIX-1364's — all built and merged.
