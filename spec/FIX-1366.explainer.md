# FIX-1366 — Teach the built-in worker kind · explainer

## 1. Today

```mermaid
flowchart LR
  R["reader wants a worker"] --> O["Workforce → Overview"]
  O --> K["example: kinds map<br/>is the required step"]
  K --> P["kind named<br/><b>worker-agent</b>"]
  P --> C["reader hand-writes a kind<br/>the default already provides"]
  B["shipped built-in<br/>kind id <b>agent</b>"] -.->|taught nowhere| C
  AT["Atlas (public Pages root)"] --> S["calls a <i>removed</i> API<br/>an 'invent-kill candidate'"]
```

The built-in worker is on `main`. Every published page still teaches the world before it: a
`kinds` map is mandatory, and the kind you must write is called `worker-agent` — a placeholder
that reads like something we ship. The executable surface renamed it to `custom-agent` so the
two could not be confused; prose did not follow, so the confusion survives only where users read.

## 2. After (proposed)

```mermaid
flowchart LR
  R["reader wants a worker"] --> O["Overview:<br/>zero-code path first"]
  O --> W["WORKER.md, no flow:<br/>→ working worker"]
  O -->|"when the default<br/>is not enough"| N["<b>new page</b><br/>The built-in worker"]
  N --> N1["what you get free"]
  N --> N2["what you can change<br/>catalog · skills · model · tools fence"]
  N --> N3["what it does not do<br/><b>no memory across turns</b>"]
  O --> F["workers-on-disk:<br/>kind named <b>custom-agent</b>"]
  AT["Atlas"] --> AF["factual pass:<br/>removed is removed"]
```

One new page carries the concept. The overview leads with the promise and demotes `kinds` to
the door you open when you need your own. The placeholder renames everywhere prose teaches it.

## 3. Where the decision fell

```mermaid
flowchart TD
  I["FIX-1366<br/>teach it"] --> D1["its own page, not a<br/>section of the overview"]
  I --> D2["publish now, stating<br/>the memory gap plainly"]
  I --> D3["Atlas: factual pass,<br/>not a restructure"]
  D1 -.->|rejected| R1["fold into overview /<br/>workers-on-disk"]
  D2 -.->|rejected| R2["hold the page until<br/>memory ships"]
  D3 -.->|rejected| R3["rebuild the Atlas around<br/>the shipped built-in"]
  I --> Z["two briefed sweeps<br/>collapsed to zero work"]
  Z --> Z1["superseded factory names:<br/>0 hits repo-wide"]
  Z --> Z2["'killed agent surface':<br/>pages are already correct —<br/>the options are still live"]
```

Three calls. The research finding worth carrying is the bottom branch: two of the four sweeps
the brief listed were already done or were never broken, which moved this issue's centre from
cleanup to the thing nobody had done — teaching the built-in at all.

_Panel 4 lands once the documentation merges._
