<svg viewBox="0 0 940 362" role="img" xmlns="http://www.w3.org/2000/svg"
     aria-label="What's in the box: the W4 first cut is a channel or org board that routes work to a seat run and assigns team seats — the exit gate, one hop — plus whatever FIX-1394's ratify records about the package format — one format with two attachment modes, or don't collapse — a runtime inventory in two layers (a declared roster composed at read time and a live org resource ChannelFlow updates), and the shipped dispatch and session policy. Composed in by the app: custom kinds as flow factories, and defaultWorker triage. A phase-2 panel inside W4, off the exit gate and holding no wrap, holds just two items: the nested personal and request board cascade, and the manager-queue lab's nested half. Below a fence, what is not built: no Agent, Channel, Team, MessageBoard, TeamFlow or SessionBoard L1 type, no Collab RC, no silent parent transcript, no nested or shadow session substrate as a work hierarchy, no merged board-assignee and seat registry, no team wildcards as first ship, no second WorkerRegistry or mega-loader, no assignable-channel routing, no BoardFlow as the only mint, no Graft rebuild.">
  <style>
    :root { --surface:#FCFCFA; --surface-2:#E8E8E1; --ink:#191C21; --ink-2:#4C535B; --ink-3:#7A828B;
            --rule:#D5D4CB; --accent:#3E4E8C; --accent-soft:#E0E3F0; --code:#2C7C69; --code-soft:#DCEDE7;
            --prose:#A8761C; --prose-soft:#F3E8D2; --gone:#A9544F; }
    @media (prefers-color-scheme: dark) {
      :root { --surface:#181C1F; --surface-2:#202528; --ink:#E6E8E5; --ink-2:#A4ACB1; --ink-3:#737B81;
              --rule:#2B3135; --accent:#8B9BD8; --accent-soft:#1B2140; --code:#5DBBA1; --code-soft:#14302A;
              --prose:#D9A44B; --prose-soft:#332612; --gone:#DE8A84; } }
    .box   { fill:var(--surface); stroke:var(--rule); stroke-width:1.2; }
    .in    { fill:var(--code-soft); stroke:var(--code); stroke-width:1; }
    .gate  { fill:var(--code-soft); stroke:var(--code); stroke-width:2.2; }
    .comp  { fill:var(--accent-soft); stroke:var(--accent); stroke-width:1; }
    .later { fill:var(--prose-soft); stroke:var(--prose); stroke-width:1; }
    .fence { stroke:var(--gone); stroke-width:1.4; stroke-dasharray:6 4; fill:none; }
    .t-lbl { fill:var(--ink-3); font-family:ui-monospace,SFMono-Regular,Menlo,monospace;
             font-size:9.5px; letter-spacing:.1em; }
    .t-gat { fill:var(--code); font-family:ui-monospace,SFMono-Regular,Menlo,monospace;
             font-size:9.5px; letter-spacing:.1em; }
    .t-b   { fill:var(--ink); font-family:system-ui,-apple-system,sans-serif; font-weight:600; font-size:13px; }
    .t-s   { fill:var(--ink-2); font-family:ui-monospace,SFMono-Regular,Menlo,monospace; font-size:9.5px; }
    .t-out { fill:var(--gone); font-family:ui-monospace,SFMono-Regular,Menlo,monospace; font-size:9.5px; }
    .t-out-l { fill:var(--gone); font-family:ui-monospace,SFMono-Regular,Menlo,monospace;
               font-size:9.5px; letter-spacing:.1em; }
  </style>

  <g id="in-the-box">
    <rect class="box" x="16" y="26" width="560" height="234" rx="5"/>
    <text class="t-lbl" x="32" y="46">IN THE BOX — THE W4 FIRST CUT</text>

    <rect class="gate" x="32" y="58" width="528" height="44" rx="4"/>
    <text class="t-gat" x="44" y="73">THE EXIT GATE</text>
    <text class="t-b" x="44" y="90">channel / org board &#8594; seat runs &#183; assign team seats</text>

    <rect class="in" x="32" y="106" width="528" height="36" rx="4"/>
    <text class="t-b" x="44" y="123">package format — open until the ratify</text>
    <text class="t-s" x="44" y="136">one format, two modes &#183; or don't collapse — whichever FIX-1394 records</text>

    <rect class="in" x="32" y="146" width="528" height="36" rx="4"/>
    <text class="t-b" x="44" y="163">runtime inventory — two layers</text>
    <text class="t-s" x="44" y="176">declared roster, composed at read time &#183; live org resource: membership &#183; fan-out &#183; DM</text>

    <rect class="in" x="32" y="186" width="528" height="36" rx="4"/>
    <text class="t-b" x="44" y="203">dispatch + session policy</text>
    <text class="t-s" x="44" y="216">parentSessionId at mint &#183; history by opt-in tools — shipped, FIX-1408</text>

    <text class="t-s" x="32" y="245">A sub-agent is same-session background; a worker assign is a seat in a linked session.</text>
  </g>

  <g id="composed-in">
    <rect class="box" x="592" y="26" width="332" height="124" rx="5"/>
    <text class="t-lbl" x="608" y="46">COMPOSED IN BY THE APP</text>
    <rect class="comp" x="608" y="58" width="300" height="36" rx="4"/>
    <text class="t-b" x="620" y="75">custom kinds</text>
    <text class="t-s" x="620" y="88">flow factories under workforce/flows/</text>
    <rect class="comp" x="608" y="100" width="300" height="36" rx="4"/>
    <text class="t-b" x="620" y="117">defaultWorker triage</text>
    <text class="t-s" x="620" y="130">who to assignTask to — composition</text>
  </g>

  <g id="phase-two">
    <rect class="box" x="592" y="162" width="332" height="98" rx="5"/>
    <text class="t-lbl" x="608" y="182">PHASE-2 — OFF THE GATE, HOLDS NO WRAP</text>
    <rect class="later" x="608" y="192" width="300" height="56" rx="4"/>
    <text class="t-s" x="620" y="215">nested personal / request boards</text>
    <text class="t-s" x="620" y="232">the manager-queue lab's nested half</text>
  </g>

  <g id="not-built">
    <path class="fence" d="M16 276 L924 276"/>
    <text class="t-out-l" x="16" y="298">NOT BUILT</text>
    <text class="t-out" x="104" y="298">no Agent / Channel / Team / MessageBoard / TeamFlow / SessionBoard as an L1 type &#183; no Collab RC as this epic</text>
    <text class="t-out" x="104" y="314">no silent parent transcript &#183; no nested / child / shadow sessions as a work hierarchy</text>
    <text class="t-out" x="104" y="330">no merged board-assignee + seat registry &#183; no team.* wildcards as first ship &#183; no second WorkerRegistry</text>
    <text class="t-out" x="104" y="346">no mega-loader &#183; no assignable-channel &#183; no BoardFlow-as-only-mint &#183; no Graft rebuild</text>
  </g>
</svg>
