# FIX-1592 · Evolution

[Spec](SPEC.md) · [Decisions](DECISIONS.md) · [Rules](BUSINESS-RULES.md) · [Plan](PLAN.md) · [Docs](DOCS.md) · **Evolution**

Only lineage that spans more than one child. FIX-1585's own predecessors are in its evolution
record.

| Prior intent and precise source | Treatment | Reason / evidence | Replacement | Compatibility |
|---|---|---|---|---|
| This epic's first version: a usable support desk, FIX-1585 → FIX-1589 → FIX-1591, FIX-1590 cut. Source: [#2265](https://github.com/fixpoint-labs/flow-state-dev/pull/2265), merged at `b6777d1`, its D1, D2, D4 and ER-1 to ER-20 | **Superseded**, except the facts below | The owner's re-scope on 2026-09-25 ([D1](DECISIONS.md#d1)) | D1 to D3 · ER-1 to ER-20 | Nothing was built against it. FIX-1589 left as a follow-on in the re-scope's first draft and is a child again, ahead of FIX-1590 ([D1](DECISIONS.md#d1)), from its own spec; its real-model goal does not come back ([D3](DECISIONS.md#d3)). FIX-1591 keeps its Linear issue, held |
| The first version's fence: the only package change is FIX-1585's transcript line (its ER-7) | **Superseded** | The owner allowed Workforce changes where a path needs them | ER-8 | The layer rule (no Workforce concept below Workforce) is retained |
| The round-1 finding that waking an agent seat needs an internal receiver. Source: [#2265 review](https://github.com/fixpoint-labs/flow-state-dev/pull/2265#discussion_r4107585826) | **Retained** | Still true of the code | ER-2 builds the receiver | — |
| `escalations` ships unattended so the boot warning is visible. Source: [FIX-1476 D3](../../issues/FIX-1476/DECISIONS.md#d3), its BR-10 | **Retained** | The first version's D3 superseded it; that D3 left with FIX-1591 | None in this set. FIX-1589 files rows onto the board and nobody drains them (ER-13) | Goal legs V8 and V9 and FIX-1455 ER-22 stand unamended |
| `desk-clerk`'s `answer` runs the `desk-note` block and hands the note back with the desk's name, no model call. Source: `apps/kitchen-sink/workforce/flows/workers/desk-clerk.ts` and `workforce/blocks/desk-note.ts`, shipped by FIX-1476 (#2007) | **Superseded** for the `answer` action | Once FIX-1585 makes the clerk reachable, a parrot looks like a working agent | ER-20, built by FIX-1589 | `answer` keeps its input, `{ note }`, so FIX-1585 D3's map and the CLI call are unchanged. `desk-note` is also a tool seats name; whether it stays is FIX-1589's spec to decide |
| The notify block names each member and wakes none; "a real app puts a dispatcher here". Source: `apps/kitchen-sink/workforce/channel-notify.ts`, shipped by FIX-1476 (#2007) | **Amended** for agent seats | Path two needs agent members to run | ER-2 · ER-3, built by FIX-1590 | Clerk and runner members keep the name-only line. Nobody is told about their own post (BR-16a, V14) |
| An agent seat's question is not kept after its reply. Source: FIX-1585 BR-14 on [`spec/FIX-1585`](https://github.com/fixpoint-labs/flow-state-dev/blob/spec/FIX-1585/specs/issues/FIX-1585/BUSINESS-RULES.md), unmerged | **Superseded** | The owner: a seat is a conversation | ER-1, folded by FIX-1585 (ER-16) | A seat conversation from before the change has replies without questions; nothing to migrate |
| No proof issue: each leg is its owner's browser goal check, all four run on one `main` commit at wrap. Source: the re-scope, [#2269](https://github.com/fixpoint-labs/flow-state-dev/pull/2269), its SPEC.md *how we verify* and ER-17 | **Amended** | [`orchestration.md`](../../../docs/contributing/orchestration.md#the-closure-issue-every-epic-ends-in-qa) now requires every epic to end in a closure issue | FIX-1601 · required; QA plan [#2288](https://github.com/fixpoint-labs/flow-state-dev/pull/2288) | The four checks and their controls are unchanged; they now run inside FIX-1601's plan |
| Path two's fan-out is kitchen-sink host glue (`notifyFor` over hired seats, a keyed dispatcher, one conversation per seat per channel, the author filter), and the closure grades it there. Source: FIX-1590's implementation, [#2290](https://github.com/fixpoint-labs/flow-state-dev/pull/2290); the closure amendment, [#2289](https://github.com/fixpoint-labs/flow-state-dev/pull/2289) | **Amended** | The owner, on #2290: every host would copy that glue, so it belongs in Workforce | [FIX-1602](https://linear.app/fixpoint-labs/issue/FIX-1602) · required, blocked by FIX-1590 and FIX-1594, blocking FIX-1601; its spec on `spec/FIX-1602` · ER-14 · ER-17 | ER-2, ER-3 and D2 are unchanged; FIX-1602 carries them into Workforce and owns no new ER. Kitchen-sink keeps its test controls and kind map (ER-10). FIX-1602's Linear text still reads *not a child*; the coordinator's wiring supersedes it |

Re-check each source against current code before implementing. No row supersedes the channel
and board convention itself (board v1, [FIX-1455 EVOLUTION](../FIX-1455/EVOLUTION.md)).
