# FIX-1549 · Evolution

[Spec](SPEC.md) · [Decisions](DECISIONS.md) · [Rules](BUSINESS-RULES.md) · [Plan](PLAN.md) · [Docs](DOCS.md) · **Evolution**

| Prior intent and precise source | Treatment | Why / evidence | Replacement | Compatibility |
|---|---|---|---|---|
| FIX-1522 F2 plan: "any other deep pattern is refused when it's defined (probe C)"; source [`../FIX-1522/F2-PLAN.md`, "The open wall: where a user-owned row lives"](../FIX-1522/F2-PLAN.md#the-open-wall-where-a-user-owned-row-lives), shipped as FIX-1529's cut C in [#2091](https://github.com/fixpoint-labs/flow-state-dev/pull/2091) (`fdca49f`) | **Amended**: the refusal is retained, moved from definition to registration, and generalized from the roster to any owner-private collection | The define-time check only ever refused a subset of what registration refuses ([poc/characterize](poc/characterize/README.md)). Running it in Core is what makes every app pay | [D1](DECISIONS.md#d1), [D4](DECISIONS.md#d4), BR-1, BR-14 | Workforce apps: the same patterns fail at startup instead of module load, with a generic message |
| FIX-1529 `ef5228f`: "a parameterised pattern that can resolve onto a user-owned roster row is refused when the flow registers", for every flow in every registry | **Amended**: retained wherever an owner-private collection is registered, for collections in its scope; not applied in a registry that never holds one | The scan refuses generic patterns in apps with no Workforce. Raised twice in FIX-1529 review, at [r4084843555](https://github.com/fixpoint-labs/flow-state-dev/pull/2091#discussion_r4084843555) and [r4084780739](https://github.com/fixpoint-labs/flow-state-dev/pull/2091#discussion_r4084780739) | [D1](DECISIONS.md#d1), BR-2 to BR-9 | Where no owner-private collection is registered, an overlapping collection is admitted but cannot read or write an owner key (BR-15 to BR-19). A collection in another scope is admitted beside one (BR-24) |
| FIX-1529's caller-only read of the branded writer (`privateRosterAdmits` in `packages/engine/src/context/hire-plane.ts`) and the brand in Core | **Superseded** by the owner-private collection | Workforce vocabulary in Layer 1 ([D3](DECISIONS.md#d3)). The same rule, generically: a row is served only to the user its owner segment names | [D4](DECISIONS.md#d4), [D5](DECISIONS.md#d5), BR-21 | Stored rows unchanged (BR-22). Refusal sentence changes |
| FIX-1529's instance pin: `InstanceOwnerPin`, `refuseInstancePin`, `pinRejectsCaller`, reasons `"owning-org"` and `"roster-owner"`, in `hire-plane.ts` | **Retained**, renamed | Generic mechanism in Workforce's words ([D6](DECISIONS.md#d6)) | S7, BR-12 | `reason` `"roster-owner"` → `"owning-user"`; exported names unchanged |
| FIX-1538's per-(org, person) cell for a pinned instance, in `packages/engine/src/stores/scope-keys.ts` | **Retained**, reworded | Keyed by the pin, not by anything Workforce names | S7 | None: no key changes |
| FIX-1549 as approved in [#2178](https://github.com/fixpoint-labs/flow-state-dev/pull/2178): D2, "the fence lives in Engine's hire-plane admission"; built as [#2196](https://github.com/fixpoint-labs/flow-state-dev/pull/2196) | **Superseded** by [D3](DECISIONS.md#d3), Jake's lock, 2026-09-24 | Hiring is not a Layer-1 concept | [D3](DECISIONS.md#d3), [D4](DECISIONS.md#d4), [PLAN → the PR plan](PLAN.md#the-pr-plan) | #2196 is closed unmerged; PR-A ports its read-path wiring and tests |

FIX-1529 has no retained spec directory; its design lives in FIX-1522's retained POC set and in
#2091. The FIX-1535 debug filter is a dependency, not a superseded design: it keeps its place and
asks the new predicate. Compare these claims against current `packages/engine/src/context/hire-plane.ts`
before building.
