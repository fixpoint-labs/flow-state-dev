# FIX-1551 · Evolution

[Spec](SPEC.md) · [Decisions](DECISIONS.md) · [Rules](BUSINESS-RULES.md) · [Plan](PLAN.md) · [Docs](DOCS.md) · **Evolution**

| Prior intent and precise source | Treatment | Why / evidence | Replacement | Compatibility |
|---|---|---|---|---|
| FIX-1442: the CLI is a trusted local caller, so it names the organization itself, `DEFAULT_ORG_ID` for both the seeded session and the run. No retained spec; source is [PR #1985](https://github.com/fixpoint-labs/flow-state-dev/pull/1985) (commit `b48158a0d`) and the comment above the seed path in `packages/cli/src/commands/run.ts` on `main` `639d12238` | **Amended.** The CLI stays a trusted in-process caller. It no longer answers for itself when the app has a resolver | POC P1: the CLI and the app's router disagree for the same app. P3: the host's own answer to the CLI's question matches HTTP | [D1](DECISIONS.md#d1); the CLI names the organization only through `--org` ([F1](DECISIONS.md#f1)) | Apps with no resolver run exactly as before (BR-2). Sessions created before stay in the placeholder organization and are refused if resumed under another (BR-11) |
| FIX-1527 VG: the success run goes through the app's HTTP router, not `fsdev run`, because the CLI pins the development organization. Source: [`../FIX-1527/PLAN.md#checks`](../FIX-1527/PLAN.md#checks), row VG | **Premise superseded once this ships.** FIX-1527's text is not edited here | This issue removes the pin that row names | This spec's VG runs the same hire through `fsdev run` | FIX-1527 may amend its VG in its own follow-up PR. Its HTTP form stays valid |
| FIX-1503: in-process `runAction`, `testFlow` and `fsdev run` stay trusted process callers with an explicit identity, not HTTP. Sources: [`../../epics/FIX-1503/SPEC.md#what-stays-as-it-is`](../../epics/FIX-1503/SPEC.md#what-stays-as-it-is), [`../../epics/FIX-1503/DECISIONS.md#decided-in-review-recorded-so-no-child-reopens-them`](../../epics/FIX-1503/DECISIONS.md#decided-in-review-recorded-so-no-child-reopens-them) | **Retained.** | The CLI still hands the runtime an explicit identity and presents no token. Only where it reads that identity from changes | — | PLAN "At implement time" names the one touchpoint: FIX-1503's default flip must leave a flow on the framework default answering the CLI with the development identity |
| FIX-1500 D6 and E3: kitchen-sink runs as one organization, set by one host-level resolver that reads nothing from the request. Sources: [`../FIX-1500/DECISIONS.md#d6`](../FIX-1500/DECISIONS.md#d6), [`../FIX-1500/DECISIONS.md#e3`](../FIX-1500/DECISIONS.md#e3) | **Retained and consumed.** | That resolver is what the CLI now asks. No second resolver, no remap | — | VG needs PR-B merged |

FIX-1529's hired-seat pin and FIX-1548's seat reachability are dependencies of the outcome, not
designs this spec changes.
