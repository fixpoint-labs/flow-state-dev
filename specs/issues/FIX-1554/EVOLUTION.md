# FIX-1554 · Evolution

[Spec](SPEC.md) · [Decisions](DECISIONS.md) · [Rules](BUSINESS-RULES.md) · [Plan](PLAN.md) · [Docs](DOCS.md) · **Evolution**

The epic records the count amendment and the lab's supersession at set level
([epic EVOLUTION](../../epics/FIX-1553/EVOLUTION.md)). This file is the kind's own lineage: the
readers the count amendment reaches, and which parts of the lab this design keeps.

| Prior intent and precise source | Treatment | Why / evidence | Replacement | Compatibility |
|---|---|---|---|---|
| A block is one of four kinds, as a closed union read at run time. Sources on `main` at `5f48575c`: `BlockKind` in `packages/core/src/types/block.ts`; `BlockTraceItem.blockKind` in `packages/contracts/src/items/types.ts`; the two local unions in `packages/engine/src/context/createExecutionContext.ts`; `executeByKind` in `packages/engine/src/execution/executeBlock.ts` (throws "Unknown block kind"); `VALID_BLOCK_KINDS` in `packages/cli/src/resolve-block.ts`; `packages/testing/src/snapshot/snapshotTrace.ts`; `packages/devtool/src/react/lib/trace-tree.ts` | **Amended**: five. Nothing else about the four changes | Owner lock on [FIX-1553](https://linear.app/fixpoint-labs/issue/FIX-1553); the epic's first evolution row | S1, S2, S7 to S10; BR-27 | Stored traces only ever hold the four values and read unchanged. A reader outside this repo that switches exhaustively on kind must add a case; the changeset says so |
| Model resolution serves generators only: `ModelResolver` returns a `GeneratorModel` (`packages/core/src/types/model.ts`), built by `createModelResolver` (`packages/core/src/models/createModelResolver.ts`) with direct-then-gateway precedence | **Retained**, and extended with an evaluation path beside it. The public `ModelResolver` type (also what `createFlowState({ modelResolver })` accepts, `packages/engine/src/flowstate/types.ts`) gains an **optional** `resolveEvaluationModel` (amendment 1) | The epic's case for a kind rests on framework-owned resolution ([epic, Decided in review](../../epics/FIX-1553/DECISIONS.md#decided-in-review)). A custom resolver replaces `models` entirely, so it must opt in rather than be bypassed ([D4](DECISIONS.md#d4)) | [D1](DECISIONS.md#d1), [D4](DECISIONS.md#d4), S2, S3, S7 | Generator resolution, intents and fallback arrays are untouched. Existing custom resolvers type-check and run generators unchanged; evaluator strings on them fail closed until they add the hook (BR-29), and evaluation model instances work (BR-30) |
| The #1903 lab's model handling at `b33a072`: `labs/typesafe-jev/src/capability.ts` treats every non-empty string as evaluation-capable and defaults to `typesafe-ai/jev`; `run-evaluate.ts` reads gateway and Jev keys from the environment and takes an `apiKey` option; `client.ts` hands strings to the SDK's global default | **Superseded** | Strings are resolved and checked, not assumed; credentials come from the app's resolver; no default model | [D1](DECISIONS.md#d1), [D2](DECISIONS.md#d2), BR-6 to BR-15 | The lab stays a draft; nothing imports it |
| The lab's answer mapping at `b33a072`: `labs/typesafe-jev/src/sdk-map.ts` lifts `providerMetadata.typesafe.confidence` into each choice and score answer; `schemas.ts` adds `model`, `usage`, `path` to the output and a `noul` alias | **Retained in part**: the confidence lift and the answer fields. **Superseded**: the extra output fields (to the trace) and `noul` | The lift is the one piece the POC showed consumers need ([epic, What the end-state POC showed](../../epics/FIX-1553/DECISIONS.md#what-the-end-state-poc-showed)) | [D3](DECISIONS.md#d3), BR-16 to BR-19 | — |
| The lab's per-call override of the question set from block input (`evaluateInputSchema.questions`) | **Superseded** by questions as a function of input and context | A question set chosen by the caller is caller-controllable input (BP-031) and loses typing | [Considered and dropped](DECISIONS.md#considered-and-dropped), BR-2 | — |

Before implementing, re-read the lab at `b33a072` rather than trusting these summaries, and
re-check the reader list against current `main` with the grep in [PLAN.md](PLAN.md#guardrails).
